{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "yarn build"
  },
  "deploy": {
    "preDeployCommand": "yarn workspace @dtc/backend medusa db:migrate && yarn workspace @dtc/backend medusa exec ./src/scripts/create-admin-from-env.js",
    "startCommand": "yarn start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
