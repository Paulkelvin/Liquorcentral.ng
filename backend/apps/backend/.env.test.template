# Used by @medusajs/test-utils (module/integration tests) to create and
# drop its own temporary test databases — separate from .env's DATABASE_URL,
# which points at the persistent dev database. See jest.config.js's
# loadEnv("test", ...) call.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
