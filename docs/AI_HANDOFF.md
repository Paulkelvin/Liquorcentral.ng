# AI Handoff Document — LiquorCentral.ng

**Status:** Approved (living, authoritative onboarding document)
**Version:** 5.8
**Owner:** Program
**Last Updated:** 2026-07-20

**This is the primary onboarding document for this project.** If you are a new developer or a new AI session, read this document in full before touching code, writing content, or answering questions about the project. Do not rely on prior chat history — none is available to you, and none should be assumed to exist. This document, and the rest of `/docs`, is the single source of truth.

> **Provenance note (read this once, then ignore it).** This project's documentation has been reconciled across unmerged git branches **three times**. First, on 2026-07-18, two independently-authored branches that had diverged from a common commit — `claude/medusa-repo-clone-ut5dl5` (Phase 0 through to completion, plus the opening of Phase 1) and `claude/ai-handoff-docs-ufdn5t` (which authored this file, then stopped) — were merged onto a single working branch, `claude/project-onboarding-status-p35u3v`, keeping `claude/medusa-repo-clone-ut5dl5`'s documentation set as the base and recovering this file from the other. Second, later the same day, `claude/project-onboarding-status-p35u3v` itself fell behind `claude/medusa-repo-clone-ut5dl5` again — a separate session had continued pushing to the latter, independently drafting and freezing specifications `07`–`11` — and was fast-forward-merged back up to match once discovered. Third, on 2026-07-19, this same `claude/medusa-repo-clone-ut5dl5` branch independently drafted its own `IMPLEMENTATION_PLANNING.md` (commit `4106b99`) before discovering `claude/project-onboarding-status-p35u3v` had, in the meantime, genuinely diverged and carried Phase 2 forward through Tier A completion and three approved Tier B modules. A true three-way merge (`git merge --no-ff`, not a fast-forward — the branches had each gained unique commits) reconciled the two: the more-advanced Tier A/Tier B lineage's `IMPLEMENTATION_PLANNING.md`, `AI_HANDOFF.md`, `README.md`, `PROJECT_STATUS.md`, and `ROADMAP.md` were kept; `DECISION_LOG.md` and `CHANGELOG.md` had a new entry prepended rather than replaced, preserving both lineages' history in full. All three reconciliations are logged in full in `DECISION_LOG.md`. **The lesson that now applies three times:** if `/docs` ever again appears sparser than this document describes, or a chat claims work exists that isn't visible here, run `git fetch` and check `git log --all` and `git branch -r` before concluding documentation is missing or fabricating a replacement — history is never rewritten on this project, so nothing is ever actually lost, only possibly unmerged or (as of the third reconciliation) genuinely diverged and requiring a real three-way merge, not just a fast-forward.

---

## Table of Contents

1. [Executive Summary](#1-executive-summary)
2. [Current Technology](#2-current-technology)
3. [Business Decisions](#3-business-decisions)
4. [Major Architecture Decisions](#4-major-architecture-decisions)
5. [Repository Structure](#5-repository-structure)
6. [Documentation Reading Order](#6-documentation-reading-order)
7. [Documentation Guide and Status Summary](#7-documentation-guide-and-status-summary)
8. [Current Project State](#8-current-project-state)
9. [Current Roadmap](#9-current-roadmap)
10. [Immediate Next Step](#10-immediate-next-step)
11. [Rules for Future AI Sessions](#11-rules-for-future-ai-sessions)
12. [Project Principles](#12-project-principles)
13. [Versioning](#13-versioning)

---

## 1. Executive Summary

**LiquorCentral** is a premium online liquor and wine retailer serving customers nationwide across Nigeria. It is not a marketplace — LiquorCentral owns its own inventory, sets its own prices, and fulfills its own orders. The brand is positioned at the premium end of the market: the experience is designed to feel trustworthy, considered, and high-confidence rather than discount-driven or transactional.

**Food Central** is a food delivery subsidiary of the same company — a product line, not a separate business, spin-off, or marketplace of independent restaurants. It shares LiquorCentral's storefront, customer identity, cart, and checkout. Unlike liquor and wine (nationwide), Food Central currently operates in **Lagos only**, cooked to order, reflecting the operational realities of same-day, perishable food delivery.

**Business model:** One company, two product lines, no third-party vendors. All inventory (liquor, wine, and food) is company-owned. All delivery is fulfilled by company-owned riders for Food Central — not a gig-economy logistics marketplace. Customers can check out as guests, must confirm legal drinking age before browsing alcohol, and can choose between same-day delivery, scheduled delivery, or pickup depending on product line and location.

**Current project phase:** **Phase 0 (Brand & Design Foundation) is complete and frozen. Phase 1 — Product Specifications is complete: all 11 specifications are Approved — Frozen at v1.0.** `PRODUCT_BLUEPRINT.md`, `BRAND_IDENTITY.md`, `EXPERIENCE_PRINCIPLES.md`, and `DESIGN_SYSTEM.md` v2.0 are approved and frozen as the project's authoritative foundation. **Phase 2 (Implementation Planning) is complete and formally concluded**, per `docs/IMPLEMENTATION_READINESS_REPORT.md` (v1.0, Approved). Engineering now proceeds **autonomously through the remaining launch-critical backend milestones**, per Paul's explicit authorization, stopping only if a business decision is required, a genuine architectural conflict is found, a contradiction with an Approved document is discovered, or Medusa itself prevents the documented architecture — not after every milestone as Milestones 1–3 required. **Milestone 1 — Backend Foundation is complete**: a Medusa v2.17.2 backend exists at `backend/apps/backend`, running on real PostgreSQL and Redis in production mode, with a Nigeria/NGN region and store seeded. **Milestone 2 — the `wine-details` module**, **Milestone 3 — the `food-details` module**, and **Milestone 4 — the `delivery-slot` module are all complete**: each implements its Approved Tier B architecture in code, validated end to end against a live server plus 50 automated tests total (including real, Redis-backed concurrency tests for delivery-slot's atomic capacity enforcement). Building `food-details` surfaced a genuine Medusa constraint (only one handler per native product-lifecycle hook), resolved by consolidating both attribute modules' hooks into one shared handler; building `delivery-slot` confirmed a workflow hook handler's compensate function genuinely participates in its parent workflow's own compensation stack, the mechanism this module's capacity-release guarantee depends on. The local payment provider and the notification provider remain not yet implemented in code — both are confirmed blocked on open business decisions (provider choice; a still-Draft Tier B document), not simply next in line. **Milestone 5 — Storefront scaffold is complete**: `storefront/` runs Medusa's official DTC Starter Next.js app (not the deprecated `nextjs-starter-medusa` repository, discovered and corrected mid-milestone), wired to the real backend's Nigeria region and a real publishable API key — a scaffold proving the connection, no LiquorCentral branding/UI applied yet. **Milestone 6 — Phase 0c: Storefront Foundation is complete**: the full `DESIGN_SYSTEM.md` §B token system, a retokenized shared UI primitive library (already used by 20+ existing components — no duplicate library created), error/empty-state/toast/form-foundation infrastructure, retokenized Navigation/Footer shells (layout only), and a Jest + React Testing Library foundation (17 tests). Real axe-core testing against the live, backend-connected storefront found and fixed two genuine pre-existing WCAG violations and surfaced one out-of-scope one, left for `04_PRODUCT_LISTING_SPECIFICATION.md`'s future implementation. No Homepage/Navigation/Search/Product Listing/Product Details/Cart/Checkout behavior was implemented — see Section 8 for full detail and `backend/README.md` / `storefront/README.md` / each module's own README for the engineering-side detail this document only summarizes. **Milestones 7 through 12 — Navigation, Homepage, Product Listing, Search, Product Details, and Cart — are now all complete** (2026-07-19), each an individual specification's real behavior built on Milestone 6's foundation, per a confirmed implementation order (Navigation → Homepage → Search → Product Listing → Product Details → Cart → Checkout → Customer Account → Food Ordering → Delivery Tracking) and a large batch of previously-open business decisions Paul supplied after Milestone 9 (payment provider Paystack, notification channels, delivery areas/pricing, and more — `DECISION_LOG.md`). See `docs/PROJECT_STATUS.md`'s Current Phase paragraph for the full per-milestone detail; this section is not repeated here in full to avoid the two documents drifting out of sync on wording.

**Product vision:** LiquorCentral is the platform Nigerians trust to deliver a bottle of wine and a home-cooked meal with the same care — curated, fast, and confident enough to buy on impulse. Every part of the product, from search to checkout, should reduce friction and increase the customer's confidence that they are buying the right product from a legitimate, premium operator.

---

## 2. Current Technology

| Technology | Role | Why it was chosen |
|---|---|---|
| **Medusa** (v2.17.2, vendored as a git submodule at `./medusa`) | Headless commerce engine — carts, checkout, orders, inventory, product catalog, customers | Open-source, self-hostable, modular by design (no core-editing required to extend it). Provides commerce fundamentals out of the box while remaining fully extensible via custom modules — necessary because the business has requirements a closed SaaS platform would make difficult or expensive: age verification, two product lines with different attribute schemas, and company-owned delivery logistics. See `ARCHITECTURE.md` and `MEDUSA_EXTENSIONS.md`. |
| **Next.js** (Medusa's official DTC Starter, at `storefront/` — Milestones 5–6) | Customer-facing storefront | Ships with cart, checkout, region/tax handling, and payment integration already wired against Medusa's API, avoiding a from-scratch reimplementation with no maintained upgrade path. SSR/SEO supports organic product discovery. Wired to the real backend and now carries the full `DESIGN_SYSTEM.md` token system and shared infrastructure (Milestone 6) — individual page specifications (Homepage, Navigation, etc.) not yet implemented. See `TECH_STACK.md` and `storefront/README.md`. |
| **Sanity** (recommended, not urgent, not yet formally approved) | Content management (marketing/editorial content) | Kept strictly separate from commerce data, synced one-way from Medusa. Real-time collaborative editing, strong localization, official Medusa integration pattern. See `MEDUSA_EXTENSIONS.md` #7. |
| **Meilisearch** (recommended, not yet formally approved) | Product search and discovery | Open-source, officially documented Medusa integration, strong faceting for wine/food attribute filtering — the lowest-integration-risk option versus Algolia/Elasticsearch/Typesense. See `MEDUSA_EXTENSIONS.md` #6. |
| **PostgreSQL** | Primary relational database (via Medusa) | Medusa's own requirement; ACID-compliant and battle-tested for transactional integrity of orders, payments, and inventory. |
| **Redis** | Caching, session storage, background jobs/event queues | Required for production Medusa (its in-memory dev mode is explicitly not production-appropriate). Keeps checkout, search, and session operations fast under load. |

**Payment provider is Paystack, and notification channels are Email + WhatsApp (mandatory) + in-app** — both approved 2026-07-19, see `DECISION_LOG.md` and `MEDUSA_EXTENSIONS.md` #4–#5. Neither blocks `ROADMAP.md` Phase 1 any longer; the notification *provider module's* own Tier B architecture document remains Draft, not yet Approved.

---

## 3. Business Decisions

The following are **finalized, non-negotiable** unless Paul explicitly revisits one (logged in `DECISION_LOG.md`). Full detail in `BUSINESS_RULES.md`.

- **Single company.** LiquorCentral is one company operating its own product lines — not a federation of independent sellers.
- **Not a marketplace.** No third-party vendors, no vendor onboarding, no vendor dashboard, no vendor payouts, no marketplace commissions.
- **Food Central is a subsidiary product line**, not a separate company or website.
- **Wine and spirits ship nationwide** across Nigeria.
- **Food Central delivers Lagos only**, and is cooked on demand (no held stock of finished dishes).
- **Same-day delivery** is a core Food Central offering.
- **Scheduled delivery** — a future date/time window — is supported.
- **Pickup** is supported, with equal visual/UX weight to delivery.
- **Company-owned riders** fulfill all Food Central delivery — no third-party courier/dispatch platform. (Wine & Spirits' nationwide delivery mechanism — in-house fleet vs. courier partner — is still open; see Section 8.)
- **Guest checkout** is required throughout — never force account creation to complete a purchase.
- **Age confirmation** is required before browsing alcohol content, not just at checkout.
- **Premium positioning.** Feel: premium, modern, elegant, fast, trustworthy, mobile-first, effortless. Every design/product decision is judged against: *does this reduce friction and increase purchase confidence?*

**Approved 2026-07-19 (full detail in `DECISION_LOG.md`):**
- **Payment provider: Paystack. No cash-on-delivery.** Build now via environment variables; test credentials to follow.
- **Notification channels: Email, WhatsApp, in-app** — Email and WhatsApp mandatory; architecture must allow adding/removing channels later without a rewrite.
- **Delivery areas at launch: Food Central within Lagos Island; Wine & Spirits across all of Lagos** (a launch-scope narrowing of the existing "nationwide" long-term intent, not a reversal — see `DECISION_LOG.md`'s note on the resulting `BUSINESS_RULES.md` tension). Configurable later without a code change.
- **Delivery pricing: distance-based, starting ~₦3,000, fully configurable, never hardcoded.**
- **Operating hours: 9:00 AM–11:00 PM initially, configurable later without a code change.**
- **Inventory: "Out of Stock" at zero stock; no backorders.**
- **Search: one unified result list across both catalogs, with a small catalog badge/tag per result; never split into separate result pages** — a presentation rule implementable against native search today, ahead of any future Meilisearch adoption.
- **Age verification (MVP): a required checkbox** ("I confirm that I am 18 years or older"), architected to be replaced by a stronger mechanism later without a major refactor.
- **Currency: NGN only** (reaffirms the existing assumption). **Tax: VAT displayed at checkout; pricing not assumed tax-inclusive.**
- **Coupons: build the capability (native Promotion Module, very likely); do not create any coupon yet.**
- **Loyalty: confirmed out of scope for MVP. Wishlist: long-term roadmap only, not prioritized ahead of launch-critical commerce work.**
- **Standing process rule: engineering now continues autonomously, milestone by milestone, without stopping for approval — interrupting only for a genuine business decision, a genuine architectural conflict, or a contradiction with an Approved document.** Missing credentials/API keys are never a reason to stop.

---

## 4. Major Architecture Decisions

Full detail in `ARCHITECTURE.md`, `MEDUSA_EXTENSIONS.md`, and `PRODUCT_BLUEPRINT.md`.

### Approved

- **One Medusa instance / one store** serves both product lines — not separate backends per business line.
- **One storefront**, one shared shell (header, search, cart, account) over two internally-distinct sections — not two microsites.
- **Two product lines** (Wine & Spirits, Food Central) as two configurations of the same Product module — not two catalog systems.
- **Two focused product-attribute modules** (`wine-details`, `food-details`), each linked 1:1 to Product via Medusa's `defineLink` — not one universal attribute module. A wine has no spice level; a dish has no vintage.
- **One cart, one checkout**, even for a mixed wine + food order — no order-splitting workflow.
- **Medusa vendored as a git submodule** at `./medusa`; **Medusa core is never modified** — all customization goes through documented extension points (custom modules, module links, workflow hooks, custom API routes, admin widgets).
- **Medusa's native auth** (customer + admin-user) is the system of record — no third-party identity provider for v1.

### Rejected / Superseded

Recorded intentionally, not deleted, so future sessions understand what was considered and explicitly ruled out. **Do not reintroduce these unless Paul explicitly changes the business model.**

- **Marketplace architecture — REJECTED/SUPERSEDED.** Earlier research explored a multi-vendor marketplace (Vendor module, vendor staff accounts, order-splitting checkout, per-vendor payouts). Retired in favor of the single-company model above. See the supersession notice at the top of `PRODUCT_BLUEPRINT.md`.
- **Vendor modules, vendor actor-type, order-splitting workflow, per-vendor payout API — REJECTED/SUPERSEDED**, along with the marketplace model that required them.

---

## 5. Repository Structure

```
/
├── README.md                          Root README — points here (/docs) as the source of truth
├── .gitmodules                        Registers the medusa/ submodule
├── medusa/                            Vendored medusajs/medusa v2 (git submodule) — never edited directly, reference only
├── backend/                           Engineering — the actual Medusa v2 application (Yarn workspaces + Turborepo)
│   ├── README.md                      What's built, what's configured, what's deliberately deferred
│   └── apps/backend/                  The Medusa v2.17.2 application (@dtc/backend) — see backend/README.md
│       ├── src/modules/wine-details/  Milestone 2 — Wine & Spirits attributes (TIER_B_WINE_ATTRIBUTES_MODULE.md), own README.md
│       ├── src/modules/food-details/  Milestone 3 — Food Central attributes (TIER_B_FOOD_ATTRIBUTES_MODULE.md), own README.md
│       ├── src/modules/delivery-slot/ Milestone 4 — bookable capacity-limited time slots (TIER_B_DELIVERY_SLOT_MODULE.md), own README.md
│       └── src/workflows/hooks/       Shared productsCreated/productsUpdated handler both attribute modules run from; complete-cart-validate.ts (delivery-slot capacity enforcement)
├── storefront/                        Milestones 5-11 — Medusa's DTC Starter Next.js app, full DESIGN_SYSTEM.md token/component foundation, and five individual specifications built on it (Navigation, Homepage, Product Listing, Search, Product Details), own README.md
└── docs/
    ├── AI_HANDOFF.md                  This file — read first
    ├── README.md                      Documentation index, reading order, continuity rules — see Section 6
    ├── DOCUMENTATION_GOVERNANCE.md    Governing standard for all documentation work — statuses, change rules, versioning, contributor rules
    ├── PROJECT_STATUS.md              Current phase, work status, blockers, open questions — always current
    ├── PRODUCT_BLUEPRINT.md           Product vision, positioning, 18 strategic pillars — frozen (Phase 0)
    ├── BRAND_IDENTITY.md              Brand vision, personality, voice, approved colors and their usage — frozen (Phase 0)
    ├── EXPERIENCE_PRINCIPLES.md       15 experience principles, product vision, competitive positioning — frozen (Phase 0)
    ├── DESIGN_SYSTEM.md               Design principles + concrete foundations (type, spacing, color tokens, etc.) v2.0 — frozen (Phase 0)
    ├── BUSINESS_RULES.md              Finalized, non-negotiable business decisions
    ├── ARCHITECTURE.md                How the vendored Medusa codebase is structured
    ├── MEDUSA_EXTENSIONS.md           Catalog of custom modules/extensions needed, with approval status
    ├── API_DECISIONS.md               Native vs. custom API routes, and why
    ├── TECH_STACK.md                  Full technology stack, confirmed vs. recommended
    ├── INFORMATION_ARCHITECTURE.md    Site/navigation structure for both product lines
    ├── PRODUCT_CATALOG.md             How Wine & Spirits and Food Central products are modeled
    ├── DELIVERY_MODEL.md              Delivery/pickup/scheduling strategy
    ├── USER_FLOWS.md                  Step-by-step customer journeys (no visuals)
    ├── BRAND_GUIDELINES.md            Placeholder — tactical asset execution (logo, exact tokens), narrower scope now that BRAND_IDENTITY.md exists
    ├── IMPLEMENTATION_PLANNING.md     Master governing document for Phase 2 — Implementation Planning
    ├── IMPLEMENTATION_READINESS_REPORT.md   Formally concludes Phase 2; authorizes the transition to implementation — Approved, v1.0
    ├── ROADMAP.md                     Phased build sequence (no dates)
    ├── DECISION_LOG.md                Append-only record of every material decision
    ├── CHANGELOG.md                   Changelog of the documentation set itself
    ├── implementation-planning/       Phase 2 — Tier A–F implementation-planning documents
    │   ├── TIER_A_FOUNDATIONAL_RECONCILIATION.md      Approved, v1.5 — complete
    │   ├── TIER_B_PRODUCT_RELATIONSHIP_MODULE.md      Approved, v1.0 — first Tier B module
    │   ├── TIER_B_WINE_ATTRIBUTES_MODULE.md           Approved, v1.0 — second Tier B module
    │   ├── TIER_B_FOOD_ATTRIBUTES_MODULE.md           Approved, v1.0 — third Tier B module
    │   ├── TIER_B_DELIVERY_SLOT_MODULE.md             Approved, v1.0 — fourth Tier B module
    │   ├── TIER_B_LOCAL_PAYMENT_PROVIDER_MODULE.md    Approved, v1.0 — fifth Tier B module
    │   └── MODULE_INVENTORY.md                        Approved, living, v1.8 — index of every module
    └── specifications/                Phase 1 — behavior-level specs for each product surface (complete)
        ├── 01_NAVIGATION_SPECIFICATION.md          Approved — Frozen, v1.0
        ├── 02_HOMEPAGE_SPECIFICATION.md             Approved — Frozen, v1.0
        ├── 03_SEARCH_SPECIFICATION.md                Approved — Frozen, v1.0
        ├── 04_PRODUCT_LISTING_SPECIFICATION.md       Approved — Frozen, v1.0
        ├── 05_PRODUCT_DETAILS_SPECIFICATION.md       Approved — Frozen, v1.0
        ├── 06_CART_SPECIFICATION.md                  Approved — Frozen, v1.0
        ├── 07_CHECKOUT_SPECIFICATION.md               Approved — Frozen, v1.0
        ├── 08_CUSTOMER_ACCOUNT_SPECIFICATION.md      Approved — Frozen, v1.0
        ├── 09_FOOD_ORDERING_SPECIFICATION.md         Approved — Frozen, v1.0
        ├── 10_DELIVERY_SPECIFICATION.md              Approved — Frozen, v1.0
        └── 11_ADMIN_WORKFLOWS_SPECIFICATION.md       Approved — Frozen, v1.0
```

**`backend/` and `storefront/` are engineering output, not documentation** — see `backend/README.md` and `storefront/README.md` for what they actually contain; this file (`AI_HANDOFF.md`) only summarizes them. As of Milestone 6, `backend/` contains the wine-details, food-details, and delivery-slot custom modules (no custom API routes yet), and `storefront/` runs Medusa's official DTC Starter wired to the real backend, now carrying the full `DESIGN_SYSTEM.md` token system, retokenized shared UI primitives, and shared infrastructure (error boundaries, empty states, toasts, form foundation) — no individual page specification (Homepage, Navigation, etc.) implemented yet.

---

## 6. Documentation Reading Order

Anyone — human or AI — picking up this project should read, in this order:

1. **This document (`AI_HANDOFF.md`)** — project-level onboarding, business model, tech stack, current phase, rules.
2. **`docs/README.md`** — the documentation index proper: full document map, the document-status convention, and continuity rules for keeping `/docs` synchronized.
3. **`docs/DOCUMENTATION_GOVERNANCE.md`** — the governing standard behind everything in step 2: the document hierarchy, lifecycle statuses, change rules, cross-reference rules, versioning rules, and the AI/human contributor rules that apply to any work on `/docs` from here on.
4. **`docs/PROJECT_STATUS.md`** — current phase in full detail, completed/in-progress/blocked work, and every decision awaiting Paul's approval.
5. **`docs/PRODUCT_BLUEPRINT.md`**, then **`docs/BRAND_IDENTITY.md`**, **`docs/EXPERIENCE_PRINCIPLES.md`**, and **`docs/DESIGN_SYSTEM.md`** — the four frozen Phase 0 documents; read together, they answer what LiquorCentral is, who it is, how it should feel to use, and what design foundations everything downstream builds on.
6. **`docs/specifications/`** — behavior-level specifications for individual product surfaces, built on the four frozen documents above. All 11 (`01`–`11`) are Approved — Frozen; read whichever ones are relevant to the task at hand.
7. **`docs/IMPLEMENTATION_PLANNING.md`** — the master governing document for Phase 2, now that Phase 1's specifications are complete. Read this before drafting any implementation-planning document.
8. **`docs/IMPLEMENTATION_READINESS_REPORT.md`** — the formal conclusion of Phase 2 and the authorization gate into implementation. Read this before beginning any engineering work.
9. **`docs/DECISION_LOG.md`** and **`docs/CHANGELOG.md`** — the authoritative history of what was decided and what changed, respectively. Read these when you need to understand *why* something is the way it is, or whether a document you're about to read is current.
10. Everything else in `docs/README.md`'s document map, as relevant to the task at hand.

---

## 7. Documentation Guide and Status Summary

`/docs` contains 25 documents plus the 11-file `specifications/` directory (36 files total). `docs/README.md` is the detailed document map with cross-links and the document-status convention; `docs/DOCUMENTATION_GOVERNANCE.md` is the governing standard behind that convention (statuses, change rules, cross-reference rules, versioning, contributor rules); this section summarizes current status by category.

### Frozen (Phase 0 output, plus all 11 Product Specifications — do not modify unless a business decision changes)

- `PRODUCT_BLUEPRINT.md` — approved and frozen, 2026-07-18.
- `BRAND_IDENTITY.md` — approved and frozen, 2026-07-18.
- `EXPERIENCE_PRINCIPLES.md` — approved and frozen, 2026-07-18.
- `DESIGN_SYSTEM.md` v2.0 — approved and frozen as the authoritative design foundation, 2026-07-18.
- `docs/specifications/01_NAVIGATION_SPECIFICATION.md` through `11_ADMIN_WORKFLOWS_SPECIFICATION.md` — all 11, Approved — Frozen at v1.0, 2026-07-18. Phase 1 — Product Specifications is complete.

### Approved (living or reference documents, not frozen)

- `docs/AI_HANDOFF.md` (this file), `docs/README.md`, `docs/PROJECT_STATUS.md`, `docs/DECISION_LOG.md`, `docs/CHANGELOG.md`, `docs/DOCUMENTATION_GOVERNANCE.md` — process/governance documents, expected to keep changing as the project moves (`DOCUMENTATION_GOVERNANCE.md` less often than the others — see its own Section 12, "Future Maintenance").
- `docs/IMPLEMENTATION_PLANNING.md` — the master governing document for Phase 2, approved 2026-07-18. Not itself an implementation document; contains no code, API design, database schema, or component design.
- `docs/implementation-planning/TIER_A_FOUNDATIONAL_RECONCILIATION.md` — Tier A, approved 2026-07-18, corrected to v1.4 (2026-07-19). Reconciles every pre-existing technical planning document against the 11 frozen specifications; produces the Definitive Implementation Baseline every later Tier B–F document references. Not itself a design or implementation document.
- `docs/implementation-planning/TIER_B_PRODUCT_RELATIONSHIP_MODULE.md` — the first Tier B module, approved 2026-07-18. Purely architectural definition of the "pairs with" cross-catalog relationship module — responsibilities, non-responsibilities, ownership, and integration points across nine consuming surfaces. No database, API, or code content.
- `docs/implementation-planning/TIER_B_WINE_ATTRIBUTES_MODULE.md` — the second Tier B module, approved 2026-07-18. Purely architectural definition of the `wine-details` module — responsibilities, the Attributes-vs-Facts boundary, ownership, and integration points across Product/Navigation/Search/Listing/Product Details/Admin Workflows. No database, API, or code content.
- `docs/implementation-planning/TIER_B_FOOD_ATTRIBUTES_MODULE.md` — the third Tier B module, approved 2026-07-19. Purely architectural definition of the `food-details` module — responsibilities, the five-way attributes/presentation/operational-status/inventory/facts distinction, ownership, and integration points across Product/Navigation/Homepage/Search/Listing/Product Details/Admin Workflows/Food Ordering. Corrected `food-details`' dependency count (seven frozen specifications, not five) during drafting. No database, API, or code content.
- `docs/implementation-planning/TIER_B_DELIVERY_SLOT_MODULE.md` — the fourth Tier B module, approved 2026-07-19. Purely architectural definition of the delivery-slot scheduling module — scheduling and capacity concepts, a conceptual time-slot lifecycle, ownership, its relationship to Medusa's native fulfillment flow, and integration points across Checkout/Food Ordering/Delivery/Admin Workflows, plus confirmed non-integrations (Cart among them). Corrected the module's dependency list (`01`,`02`,`07`,`09`,`10`,`11`, not `06`,`07`,`09`,`10`) during drafting; its finalization review corrected an incorrect Tier-placement claim, a misattributed quotation, and an ambiguous section citation. Names, but does not resolve, two architectural questions: the pickup-slot mechanism boundary, and how kitchen-capacity and rider-capacity constraints reconcile into one tracked capacity figure. No database, API, or code content.
- `docs/implementation-planning/TIER_B_LOCAL_PAYMENT_PROVIDER_MODULE.md` — the fifth Tier B module, approved 2026-07-19. Purely architectural, provider-agnostic definition of the local payment provider module — responsibilities, non-responsibilities, the payment-method/mechanism/status/refund-policy boundary, payment lifecycle and payment-state responsibilities, provider abstraction responsibilities, ownership, its interaction with Medusa's native Payment module, and integration points across Checkout/Customer Account/Order Management/Admin Workflows, plus a dedicated Refund Workflow boundary section and confirmed non-integrations. Verified its dependency count (`07` only) requires no correction; its finalization review corrected a stale self-referential status claim, a reversed-order misquotation, a misattributed principle, and three incorrect self-references. No specific payment provider is named anywhere. No database, API, or code content.
- `docs/implementation-planning/MODULE_INVENTORY.md` — living, approved 2026-07-18, corrected to v1.8 (2026-07-19). Single-page index of every module/integration/capability identified anywhere in `/docs`, with type, status, dependencies, and launch-criticality.
- `docs/IMPLEMENTATION_READINESS_REPORT.md` — v1.0, Approved, 2026-07-19. Formally concludes Phase 2 — Implementation Planning: summarizes completed planning work, explains why Tier C (API Contract Planning) is intentionally not being produced, lists the remaining business decisions engineering cannot resolve, classifies every planning output as ready-now/ready-once-decided/deferred-until-after-MVP, recommends an implementation order, and states remaining implementation risks and mitigations. A governance record, not an architecture document — contains no new API design, database schema, or code. Modified only in response to a new business or architecture decision, per `DOCUMENTATION_GOVERNANCE.md` §5.
- `BUSINESS_RULES.md` — finalized business decisions, changes only if Paul explicitly revisits a rule.
- `ARCHITECTURE.md` — reference document, changes only if the Medusa version or conventions change materially.

### In Progress

- None. No Product Specification and no further implementation-planning document is currently in progress.

### Not Started

- No Product Specification placeholders remain — all 11 are frozen. Beyond Tier A (Approved) and five Tier B modules Approved (see Completed, above), no further Phase 2 implementation-planning document (per `IMPLEMENTATION_PLANNING.md` §4) has been started; none begins without Paul's explicit direction to begin that specific document.

### Draft (strategy generally confirmed; specific values or formal sign-off still open)

- `TECH_STACK.md` — backend confirmed and vendored; storefront/search/CMS recommendations not yet formally signed off.
- `MEDUSA_EXTENSIONS.md`, `API_DECISIONS.md`, `INFORMATION_ARCHITECTURE.md`, `PRODUCT_CATALOG.md`, `DELIVERY_MODEL.md`, `USER_FLOWS.md`, `ROADMAP.md` — see each document's own status header for exactly what remains open.
- `BRAND_GUIDELINES.md` — placeholder; narrower scope now that `BRAND_IDENTITY.md` exists; can begin whenever design capacity is available (no longer a hard blocker).

### Superseded

- An earlier multi-vendor marketplace architecture (Vendor module, vendor staff accounts, order-splitting checkout, per-vendor payouts) — retired in favor of the single-company model. See `PRODUCT_BLUEPRINT.md`'s supersession notice and `DECISION_LOG.md`.

| Document | Purpose | Owner | Update when |
|---|---|---|---|
| `docs/AI_HANDOFF.md` (this file) | Project-level onboarding; single source of truth entry point | Paul (final approver); maintained by whichever session makes the last approved change | Immediately on any business, architecture, tech-stack, or phase change |
| `docs/README.md` | Documentation index, reading order, continuity rules | Same | Whenever a document is added/removed/restructured |
| `docs/DOCUMENTATION_GOVERNANCE.md` | Governing standard: hierarchy, lifecycle statuses, change rules, cross-reference rules, versioning rules, AI/human contributor rules, workflow, quality checklist, audit process | Program | Rarely — see its own Section 12, "Future Maintenance" |
| `docs/PROJECT_STATUS.md` | Current phase, completed/in-progress/blocked work, open questions | Whoever is actively working; approved by Paul | Every session — "an out-of-date status is a bug" |
| `docs/PRODUCT_BLUEPRINT.md` | Product vision, positioning, and 18 strategic pillars | Paul (approved, frozen) | Only if a strategic/product decision changes |
| `docs/BRAND_IDENTITY.md` | Brand vision, personality, voice, approved colors and their usage | Paul (approved, frozen) | Only if a business decision changes |
| `docs/EXPERIENCE_PRINCIPLES.md` | How every screen/feature/interaction should feel | Paul (approved, frozen) | Only if a business decision changes |
| `docs/DESIGN_SYSTEM.md` | Design principles and concrete foundations (type, spacing, color tokens, motion, etc.) | Design/Paul (approved, frozen, v2.0) | Only if a business decision changes |
| `docs/BUSINESS_RULES.md` | Finalized, non-negotiable business decisions | Paul | Only when Paul explicitly revisits a rule |
| `docs/ARCHITECTURE.md` | How the vendored Medusa codebase is structured, and the module-isolation rule | Engineering | When the Medusa version changes materially, or conventions change |
| `docs/MEDUSA_EXTENSIONS.md` | Authoritative catalog of every custom module/extension needed, with risk and approval status | Engineering; per-item approval from Paul | Before building anything not already listed here |
| `docs/API_DECISIONS.md` | Native vs. custom API routes, and why | Engineering | Whenever a new route is anticipated or built |
| `docs/TECH_STACK.md` | Full technology stack end to end, confirmed vs. recommended | Engineering; sign-off from Paul | Any stack component change, logged in `DECISION_LOG.md` |
| `docs/INFORMATION_ARCHITECTURE.md` | Site/navigation structure for both product lines | Product; merchandising input from Paul | When IA/category structure changes |
| `docs/PRODUCT_CATALOG.md` | How Wine & Spirits and Food Central products are modeled; proposed attribute field lists | Paul/merchandising (field lists); engineering (structure) | When attribute field lists are finalized or changed |
| `docs/DELIVERY_MODEL.md` | Delivery/pickup/scheduling strategy for both product lines | Paul (ops decisions); engineering (mechanics) | When delivery/ops decisions change |
| `docs/USER_FLOWS.md` | Step-by-step customer journeys (no visuals) | Product | When a flow's steps change |
| `docs/BRAND_GUIDELINES.md` | Tactical asset execution (logo, exact typefaces/tokens, physical branding) | Paul / a designer, once engaged | Once brand execution work begins |
| `docs/specifications/` | Behavior-level specs for each product surface (complete, all 11 frozen) | Product; per-file approval from Paul | Only in response to a new business decision, per `DOCUMENTATION_GOVERNANCE.md` §5 |
| `docs/IMPLEMENTATION_PLANNING.md` | Master governing document for Phase 2: hierarchy, dependencies, creation order, approval workflow, exit criteria | Program/Engineering; approved by Paul | Only in response to a new business decision, or when a real gap is found running Phase 2 (see its own Section 12) |
| `docs/IMPLEMENTATION_READINESS_REPORT.md` | Formally concludes Phase 2 and authorizes the transition into implementation; lists remaining business decisions and the recommended build order | Program; approved by Paul | Only in response to a new business or architecture decision, per `DOCUMENTATION_GOVERNANCE.md` §5 |
| `docs/ROADMAP.md` | Phased build sequence (no dates) | Paul/engineering | When sequencing or phase scope changes |
| `docs/DECISION_LOG.md` | Append-only record of every material decision: what, why, when, impact, status | Whoever makes/logs the decision | Every material decision — append a new entry, never edit an old one |
| `docs/CHANGELOG.md` | Changelog of the documentation set itself (not the product) | Whoever adds/removes/restructures a document | Whenever `/docs` itself changes |

---

## 8. Current Project State

*(Condensed from `docs/PROJECT_STATUS.md` — that file is the detailed, always-current version. If the two ever disagree, `docs/PROJECT_STATUS.md` wins and this section should be updated to match.)*

**Completed**
- Business model finalized: single company, no marketplace, no vendors.
- `PRODUCT_BLUEPRINT.md`, `BRAND_IDENTITY.md`, `EXPERIENCE_PRINCIPLES.md` approved and frozen.
- `DESIGN_SYSTEM.md` v2.0 finalized and frozen as the authoritative design foundation (typography, spacing, grid, elevation, a three-tier Color Architecture, motion, breakpoints, form behaviors, accessibility tokens, Future Theme Support, Component Philosophy, Design Quality Checklist).
- Full architecture, technology, and UX/product research completed and distilled into `ARCHITECTURE.md`, `TECH_STACK.md`, `MEDUSA_EXTENSIONS.md`, `USER_FLOWS.md`, `DELIVERY_MODEL.md`.
- Medusa v2.17.2 vendored as a git submodule.
- **Phase 1 — Product Specifications is complete.** All 11 specifications (`01_NAVIGATION_SPECIFICATION.md` through `11_ADMIN_WORKFLOWS_SPECIFICATION.md`) are Approved — Frozen at v1.0, each drafted in full and either refined-then-frozen or drafted-and-frozen-directly per Paul's explicit instruction each time. Full content summaries for each are in `PROJECT_STATUS.md`'s Completed work section.
- **`IMPLEMENTATION_PLANNING.md` created and approved (2026-07-18)** — the master governing document for Phase 2, defining a six-tier hierarchy of future implementation-planning documents, their dependencies, creation order, approval workflow, and exit criteria before development begins. Contains no implementation code, API design, database schema, or component design itself.
- **Tier A — Foundational Reconciliation complete (2026-07-18)** — `docs/implementation-planning/TIER_A_FOUNDATIONAL_RECONCILIATION.md` (v1.0, Approved) reviewed all 11 frozen specifications against every pre-existing technical planning document. Confirmed every foundational architectural assumption still valid; found no obsolete assumption; found one genuine conflict (the "pairs with" relationship's framing in `PRODUCT_CATALOG.md` vs. six specifications' treatment of it as a general module); consolidated missing-work findings into a 20-row Definitive Implementation Baseline, most notably that the "pairs with" module has no `MEDUSA_EXTENSIONS.md` entry at all despite being depended on by six specifications. Did not redesign the product, modify any Frozen document, design an API, design a database, or write code. See `PROJECT_STATUS.md` for the full findings.
- **First Tier B module — Product Relationship Module — Approved at v1.0 (2026-07-18)** — `docs/implementation-planning/TIER_B_PRODUCT_RELATIONSHIP_MODULE.md`, a purely architectural definition (responsibilities, non-responsibilities, ownership, integration points across nine consuming surfaces, future extensibility) drafted, then reviewed against `IMPLEMENTATION_PLANNING.md`, `TIER_A_FOUNDATIONAL_RECONCILIATION.md`, `PRODUCT_BLUEPRINT.md`, `BUSINESS_RULES.md`, `MEDUSA_EXTENSIONS.md`, and all 11 frozen specifications before finalizing. One cross-reference gap found and closed (a new Integration with Navigation section); no existing section rewritten; no database, API, or code introduced anywhere. Two dependencies recorded, not resolved: the module's own staff curation workflow, and its still-outstanding `MEDUSA_EXTENSIONS.md` registration.
- **`docs/implementation-planning/MODULE_INVENTORY.md` created (2026-07-18)**, per Paul's explicit recommendation — a living, single-page index of every module/integration/capability identified anywhere in `/docs`, answering at a glance which are native, custom, provider, or integration; which depend on others; which are optional; and which are launch-critical.
- **Second Tier B module — Wine & Spirits Attributes Module — drafted then Approved (2026-07-18)** — `docs/implementation-planning/TIER_B_WINE_ATTRIBUTES_MODULE.md` (v1.0, status Approved), selected over the tied-on-spec-count `food-details` module via architectural centrality and `ROADMAP.md`'s own backend-phase ordering (wine-attributes is Phase 2, immediately after Wine & Spirits' Phase 1 launch; food-attributes is Phase 3). Defines the module's responsibilities, the Attributes-vs-Facts boundary against `05_PRODUCT_DETAILS_SPECIFICATION.md`, ownership (a three-way engineering/Paul/staff split), and integration points across Product, Navigation, Search, Listing, Product Details, and Admin Workflows, with Cart/Checkout/Account/Food Ordering/Delivery/Homepage confirmed as non-dependents. A finalization review pass found and fixed two ambiguous cross-references and one overstated dependency claim, added a reviews-vs-attributes boundary clarification and a product-vs-variant-granularity clarification, and corrected a stale dependency list in `TIER_A_FOUNDATIONAL_RECONCILIATION.md` (then v1.1). Two gaps recorded, not resolved: wine-attribute-accuracy verification ownership, and product-vs-variant granularity. Full selection and review reasoning in `DECISION_LOG.md`.
- **Third Tier B module — Food Attributes Module — drafted then Approved (2026-07-19)** — `docs/implementation-planning/TIER_B_FOOD_ATTRIBUTES_MODULE.md` (v1.0, status Approved), drafted after re-verifying `food-details` remains the correct next module per `IMPLEMENTATION_PLANNING.md` §6's ordering. That re-verification found `food-details`' true dependency count had been undercounted — **seven frozen specifications (`01`,`02`,`03`,`04`,`05`,`09`,`11`), not five** — since `01_NAVIGATION_SPECIFICATION.md` and `02_HOMEPAGE_SPECIFICATION.md` both genuinely depend on it for prep-time/availability data; corrected in `TIER_A_FOUNDATIONAL_RECONCILIATION.md` (now v1.2) and `MODULE_INVENTORY.md` (now v1.4). Defines the module's responsibilities; a dedicated five-way distinction between attributes, presentation, operational status, inventory, and customer-facing facts (per direct instruction) — critically, that this module never holds Food Central's live availability-state flag, a separate mechanism; ownership (a four-way engineering/Paul-field-list/staff/allergen-accuracy-ownership split, one dimension more than wine's); and integration points across Product, Navigation, **Homepage** (a genuine dependency, unlike wine's confirmed non-dependency), Search, Listing, Product Details, Admin Workflows, and Food Ordering. A finalization review pass added a new Integration with Food Ordering section (closing a missing-integration-point gap, since `09_FOOD_ORDERING_SPECIFICATION.md` — this module's single most consequential consuming specification — had no dedicated section of its own), corrected five internal cross-references, removed a reference to a nonexistent section, and added two small clarifications (a pricing non-responsibility; an optional Product Tags mirror for dietary flags). Surfaces one newly discovered field-list gap (portion/serving-size) and elevates the already-flagged, safety-critical allergen-accuracy-ownership decision as this module's own central risk, neither resolved. Full selection and review reasoning in `DECISION_LOG.md`.
- **Fourth Tier B module — Delivery-Slot Module — drafted then Approved (2026-07-19)** — `docs/implementation-planning/TIER_B_DELIVERY_SLOT_MODULE.md` (v1.0, status Approved), drafted after re-applying the six standing selection criteria against every remaining candidate (delivery-slot scheduling, the local payment provider, the notification provider, Saved-for-Later): delivery-slot wins on dependency count, architectural centrality, and `IMPLEMENTATION_PLANNING.md` §6's explicit ordering. That drafting re-verification found the module's dependency list mis-attributed — **the true dependent list is `01`,`02`,`07`,`09`,`10`,`11` (six specifications), not `06`,`07`,`09`,`10`** — since `06_CART_SPECIFICATION.md` has no genuine Backend Requirements dependency (it defers all slot mechanics to checkout) while `01_NAVIGATION_SPECIFICATION.md`, `02_HOMEPAGE_SPECIFICATION.md`, and `11_ADMIN_WORKFLOWS_SPECIFICATION.md` each have a genuine one; corrected in `TIER_A_FOUNDATIONAL_RECONCILIATION.md` (now v1.3) and `MODULE_INVENTORY.md` (now v1.6). Defines the module's responsibilities and non-responsibilities; scheduling and capacity concepts; a conceptual time-slot lifecycle; a three-way ownership split (module architecture/operational parameters/day-to-day capacity adjustment); its relationship with Medusa's native fulfillment flow (linked to Shipping Options via `defineLink`, enforced via a workflow hook, reusing existing locking); and integration points across Checkout (the primary consumer), Food Ordering, Delivery, and Admin Workflows, with Cart confirmed as a non-dependent. A finalization review pass found and corrected three genuine issues in place: an incorrect Tier-placement claim in §0 (the payment/notification provider modules are genuine Tier B candidates per `IMPLEMENTATION_PLANNING.md` §4/§6, not Tier D alone — delivery-slot still proceeds first per §6 point 2's explicit sequencing within Tier B), a misattributed quotation in §2 (corrected to `MEDUSA_EXTENSIONS.md` #3), and an ambiguous section citation in §12 (corrected to name `07_CHECKOUT_SPECIFICATION.md`'s unnumbered Customer Decision States appendix accurately). Names, but does not resolve, two architectural questions: whether pickup should ever use this module's capacity-booked mechanism rather than remain a simpler ready-time estimate, and how kitchen-capacity and rider-capacity constraints reconcile into one tracked capacity figure. Full selection and review reasoning in `DECISION_LOG.md`.
- **Fifth Tier B module — Local Payment Provider Module — drafted then Approved (2026-07-19)** — `docs/implementation-planning/TIER_B_LOCAL_PAYMENT_PROVIDER_MODULE.md` (v1.0, status Approved), drafted after re-applying the six standing selection criteria against every remaining candidate (the local payment provider, the notification provider, Saved-for-Later): the local payment provider wins on launch criticality (the project's sole confirmed launch-blocking decision) and architectural centrality (no order, of either catalog, can be placed without it), despite the lowest raw dependency count of any Tier B module drafted so far. Verified this module's dependency count directly against every specification's own Backend Requirements table and **found it requires no correction** — `07_CHECKOUT_SPECIFICATION.md` remains the sole genuine dependent, the first Tier B module whose baseline needed no swap-or-add adjustment. Surfaced one newly-discovered documentation gap: `11_ADMIN_WORKFLOWS_SPECIFICATION.md` §11 names a staff-facing refund workflow without naming this module as its underlying mechanism — recorded as a completeness recommendation, not resolved. Purely architectural and deliberately provider-agnostic throughout — no payment service provider is named or chosen anywhere, preserving the boundary between this Tier B document and a future Tier D provider-integration document. Defines the module's responsibilities and non-responsibilities; a dedicated boundary distinguishing payment method selection, this module's own mechanism, payment status on the Order, and refund policy as four separate concepts; payment lifecycle and payment-state responsibilities (reusing `07_CHECKOUT_SPECIFICATION.md`'s own Payment State Behaviour vocabulary directly); provider abstraction responsibilities; a two-way ownership split; its interaction with Medusa's native Payment module; integration points across Checkout, Customer Account, Order Management, and Admin Workflows; a dedicated Integration with Refund Workflow section stating the architectural boundary explicitly; and a consolidated non-integrations section. A finalization review pass found and corrected four genuine issues in place, none requiring a new section: a stale self-referential status claim in §0 (describing `TIER_A_FOUNDATIONAL_RECONCILIATION.md` Row 4 as "Scoped, not built" without acknowledging the same document's own drafting had updated it to "Drafted"), a reversed-order misquotation of `MEDUSA_EXTENSIONS.md` #4 in §8, a principle misattributed jointly to `PRODUCT_BLUEPRINT.md` §9 and `07_CHECKOUT_SPECIFICATION.md` §21 in §14 (corrected to `07`'s §21 alone), and three incorrect self-references citing "§18" (CMS & Merchandising, unrelated) instead of §19 (Operational Assumptions) in §4/§10/§23. Full selection and review reasoning in `DECISION_LOG.md`.
- **Remaining-Tier-B-modules recommendation approved by Paul (2026-07-19)** — per his direct instruction not to continue drafting Tier B documents purely for completeness, a review of `IMPLEMENTATION_PLANNING.md`, `MODULE_INVENTORY.md`, `PROJECT_STATUS.md`, and all five Approved Tier B documents found neither remaining candidate module (notification provider, Saved-for-Later) confirmed launch-blocking by any existing document. Implementation Planning did not continue to a sixth Tier B document.
- **Implementation-readiness assessment approved by Paul (2026-07-19)** — reviewed `IMPLEMENTATION_PLANNING.md`, `MODULE_INVENTORY.md`, `ROADMAP.md`, `PROJECT_STATUS.md`, `TIER_A_FOUNDATIONAL_RECONCILIATION.md`, all five Approved Tier B documents, `ARCHITECTURE.md`, `API_DECISIONS.md`, and all 11 frozen specifications, concluding no Tier C (API Contract Planning) document is required before implementation begins — `API_DECISIONS.md` already names only two anticipated custom routes platform-wide, `IMPLEMENTATION_PLANNING.md` §10's Exit Criteria treats Tier B "and/or" Tier C as sufficient, and the Approved Tier B documents' own Integration sections already do most of what Tier C would restate.
- **`docs/IMPLEMENTATION_READINESS_REPORT.md` created (v1.0, Approved, 2026-07-19)** — formally concludes Phase 2 — Implementation Planning and authorizes the transition into implementation. A governance record only, containing no new architecture, API design, database schema, or code. Summarizes completed planning work; explains why Tier C is intentionally not being produced; lists remaining business decisions engineering cannot resolve, grouped by urgency; classifies every planning output as Ready to implement immediately / Ready once business decisions are supplied / Deferred until after MVP; recommends an implementation order (resolving `TIER_A_FOUNDATIONAL_RECONCILIATION.md` §13's flagged Customer-Account/Admin-Workflows sequencing gap by recommending both be built in parallel with whichever phase supplies their data); and states remaining implementation risks and mitigations. Per Paul's explicit instruction, implementation does not begin automatically — it awaits his explicit approval following this report.
- **Engineering phase begun; Milestone 1 — Backend Foundation complete (2026-07-19)** — Paul approved beginning engineering directly against `IMPLEMENTATION_READINESS_REPORT.md`, `IMPLEMENTATION_PLANNING.md`, `ARCHITECTURE.md`, `API_DECISIONS.md`, `MODULE_INVENTORY.md`, `MEDUSA_EXTENSIONS.md`, and all 11 frozen specifications, with the explicit instruction to work in small, reviewable milestones and stop for approval after each. No genuine implementation conflict was found during review. Milestone 1 scaffolded a Medusa v2.17.2 backend at `backend/apps/backend` (Yarn workspaces + Turborepo, per `TECH_STACK.md`), wired to real PostgreSQL and Redis in production mode (event bus, workflow engine, locking, and cache all Redis-backed, per `ROADMAP.md` Phase 1's explicit instruction), with a LiquorCentral-specific seed replacing the generator's generic demo data: a "LiquorCentral" store (NGN), a "LiquorCentral Storefront" sales channel, a "Nigeria" region (using Medusa's built-in placeholder payment provider, not the real local provider — that remains separate, later, Paul-gated work), and a Nigeria tax-region shell with **no rate set** (an open business/legal decision, newly recorded). No stock locations, fulfillment configuration, or product data were seeded — out of scope for backend foundation. Validated against real, running Postgres/Redis: migrations completed, an admin user was created, `tsc --noEmit` passed cleanly, `medusa build` succeeded (backend and admin dashboard), the dev server booted with all four Redis connections confirmed, `GET /health` returned `200`, admin login succeeded, and the seeded region/store/tax-region were confirmed correct via the Admin API. Full detail in `backend/README.md` and `DECISION_LOG.md`.
- **Engineering Milestone 2 — `wine-details` module implemented (2026-07-19)** — per Paul's direction and a stated document precedence order (`TIER_B_WINE_ATTRIBUTES_MODULE.md` > `MODULE_INVENTORY.md` > `IMPLEMENTATION_READINESS_REPORT.md` > `ARCHITECTURE.md` > `API_DECISIONS.md` > `MEDUSA_EXTENSIONS.md` > `PRODUCT_CATALOG.md` > relevant frozen specifications), implemented `TIER_B_WINE_ATTRIBUTES_MODULE.md`'s Approved architecture in code: a custom module (data model, service, migration) linked 1:1 to Product via `defineLink`; writes via the native `POST /admin/products`/`POST /admin/products/:id` endpoints' `additional_data` extension (a zod-validated middleware plus four workflow steps, two workflows, and two hook registrations) — no new API route, matching `API_DECISIONS.md`'s anticipated shape; reads via Medusa's Query system; and a product-detail-page admin widget. No architectural conflict was found. The field list is explicitly provisional (drawn from `PRODUCT_CATALOG.md`'s own "proposed, not finalized" list), handled the same way the project already handles the open payment-provider decision — built now, flagged as open, not invented or expanded. One genuine bug (`dismissRemoteLinkStep` needing both sides of the link) was found and fixed during live verification. Validated end to end against a real running server and a real isolated test database: typecheck and lint clean, 16 automated tests passing, a full production build, and the complete create/read/update/clear-to-delete/re-create lifecycle exercised via the live Admin API. Two items recorded, not resolved: no product-deletion cascade cleanup exists yet, and the field list/variant-granularity/accuracy-ownership questions `TIER_B_WINE_ATTRIBUTES_MODULE.md` already named remain open. Full detail in `docs/DECISION_LOG.md` and `backend/apps/backend/src/modules/wine-details/README.md`.
- **Engineering Milestone 3 — `food-details` module implemented (2026-07-19)** — per Paul's direction and the same document precedence discipline as Milestone 2, implemented `TIER_B_FOOD_ATTRIBUTES_MODULE.md`'s Approved architecture in code: a custom module (data model, service, migration) linked 1:1 to Product; writes via the native product endpoints' `additional_data` extension; reads via Query; a product-detail-page admin widget. Deliberately reuses `wine-details`' pattern, not its content — no product-vs-variant question (settled, per `TIER_B` §7); a new `safety_data_verified` field making allergen/dietary-flag verification state visible (`TIER_B` §7's explicit design instruction, not an invention); and a `portion_size` field carried forward from a frozen-spec gap `TIER_B` itself names. No architectural conflict was found with any Approved document. Building it surfaced a genuine Medusa framework constraint — only one handler may be registered per native product-lifecycle hook — requiring `wine-details`' and `food-details`' hook logic to be consolidated into one shared handler (`src/workflows/hooks/`); a mechanical correction, not a business or architecture decision. Validated end to end against a real running server and a real isolated test database: typecheck and lint clean, 19 new automated tests passing plus all 16 of `wine-details`' original tests reconfirmed (35 total, zero regression), a full production build, and the complete lifecycle exercised via the live Admin API — including the specific test of one product carrying both wine-details and food-details data in a single request, confirming the consolidated hook runs both modules' workflows correctly. Two items recorded, not resolved: the same product-deletion-cascade limitation already noted for `wine-details` now applies to `food-details` too, and the field list/allergen-accuracy-ownership questions `TIER_B_FOOD_ATTRIBUTES_MODULE.md` already named remain open — the latter explicitly safety-critical. Full detail in `docs/DECISION_LOG.md` and `backend/apps/backend/src/modules/food-details/README.md`.
- **Engineering Milestone 4 — `delivery-slot` module implemented (2026-07-19)**, under Paul's autonomous-continuation authorization (see Section 1) — implemented `TIER_B_DELIVERY_SLOT_MODULE.md`'s Approved architecture in code: a custom module (time-window model, `MedusaService`-based service, migration) linked to Fulfillment's Shipping Option via `defineLink` (many slots to one option, not 1:1); atomic capacity check-and-increment reusing Medusa's own Locking Module (`locking-redis`, no new concurrency mechanism); a delivery slot attached to a cart via the native `data` JSON field on the cart's shipping method (no new Cart field); and capacity enforcement wired into `completeCartWorkflow` via its `validate` hook, registered with both an invoke and a **compensate** function. No architectural conflict was found with any Approved document. Direct source inspection of `@medusajs/core-flows` confirmed three available native hooks (`validate`, `beforePaymentAuthorization`, `orderCreated`); `validate` was chosen because it rejects an at-capacity slot before order creation, inventory reservation, or payment authorization begin. Direct source inspection of `@medusajs/workflows-sdk`'s `createHook` confirmed a hook's compensate function is wired into the same invoke/compensate step machinery an ordinary `createStep` gets — meaning it genuinely participates in the parent workflow's own compensation stack, not an approximation of that guarantee. Validated end to end: typecheck clean, 15 new automated tests passing (50 total, zero regression), and — the specific validation this module's single highest-severity named risk required — **three real, Redis-backed concurrency tests** (not mocked) confirming exactly one-of-two, exactly three-of-eight, and a correct release-then-rebook, against this environment's actual Postgres and Redis. `medusa develop` was also confirmed to boot cleanly with the hook registered. Three items recorded, not resolved: the operational parameters (`TIER_B` §9/§18/§19/§21) already named remain open; the pickup-slot boundary and kitchen-vs-rider capacity reconciliation questions remain unresolved; and a narrow residual compensation-gap edge case is named honestly in the module's own README. Full detail in `docs/DECISION_LOG.md` and `backend/apps/backend/src/modules/delivery-slot/README.md`.
- **Engineering Milestone 5 — storefront scaffold stood up (2026-07-19)**, after confirming Milestones 5/6 (original numbering — local payment provider, notification provider) were both genuinely blocked on open business decisions (see the Blocked list below) — implemented `storefront/`, Medusa's official DTC Starter Next.js app, wired to the real backend's Nigeria region and a real publishable API key scoped to the seeded sales channel. The first clone attempt (`medusajs/nextjs-starter-medusa`) was discovered mid-setup to be deprecated in favor of `medusajs/dtc-starter` — discarded and replaced before any tracking document referenced it. Only the DTC Starter's `apps/storefront` was kept; its bundled `apps/backend` was discarded as redundant with this project's own already-built backend (confirmed by both sharing the identical `@dtc/backend` package name). Six narrow, mechanical type/lint issues in the vendored template were fixed (no business logic or UI touched) — see `storefront/README.md`. Validated end to end against the real running backend: `tsc`/`lint` clean, a full production build succeeds, and both dev and production servers were exercised live, rendering real Store API data for the seeded Nigeria region. No LiquorCentral branding or custom UI applied — scaffold only. Full detail in `docs/DECISION_LOG.md` and `storefront/README.md`.
- **Engineering Milestone 6 — Phase 0c, Storefront Foundation (2026-07-19)**, under Paul's explicit directive to build the reusable technical foundation every customer-facing surface will share, before any individual specification (Homepage, Navigation, etc.) is implemented — retokenized `storefront/` end to end against `DESIGN_SYSTEM.md`'s full 3-tier color architecture, typography, spacing, radius, elevation, and motion scales (CSS custom properties in `globals.css`, referenced by `tailwind.config.js` so Tailwind utilities and raw CSS share one source, preserving the documented future-theme-swap property); rewrote the shared `Text`/`Heading`/`Button`/`Container`/`Badge`/`IconBadge`/`IconButton`/`Label`/`Input`/`Table`/`RadioGroup`/`Checkbox` primitives onto semantic tokens (static lookup objects, not template-literal class construction, to satisfy Tailwind's JIT scanner); applied LiquorCentral branding to the Nav and Footer shells (layout only, no `01_NAVIGATION_SPECIFICATION.md` behavior) and removed the vendored Medusa/Next.js promotional chrome (`MedusaCTA`, the GitHub/Docs links column, unused icon components); built new shared infrastructure not previously present — error-boundary (`error.tsx` + `global-error.tsx`), empty-state, toast/notification (`ToastProvider`, `aria-live="polite"`), a reusable form-field/blur-validation foundation, and a real `<main>` landmark + skip-link on every route-group layout (fixing a pre-existing duplicate/missing-landmark bug in the vendored template); and added a Jest + Testing Library testing foundation (17 tests). No Homepage, Navigation, Search, Product Listing, Product Details, Cart, or Checkout *behavior* was implemented or invented, and no Frozen specification was modified. Validated end to end against the real running backend: `tsc`/`lint`/`yarn test`/production build all clean; responsive behavior confirmed via real-browser viewport checks; and a live axe-core scan against the rendered homepage found and fixed two genuine WCAG violations (a nested-interactive `<button>`/`<a>` pair and the resulting invalid `aria-expanded` on a bare `<span>`) by rendering Headless UI's `PopoverButton` `as={LocalizedClientLink}` once its click-state was confirmed unused by the cart dropdown's actual open/close logic. One pre-existing, out-of-scope color-contrast issue in the vendored Product Listing "Sort by" control was found and deliberately left unfixed (page-specific behavior, not shared infrastructure); Storybook was deliberately deferred as not yet warranted by the component count. Full detail in `storefront/README.md` and `docs/DECISION_LOG.md`.
- **Repository reconciliation, three times:** first and second, 2026-07-18 — two unmerged documentation branches (`claude/medusa-repo-clone-ut5dl5` and `claude/ai-handoff-docs-ufdn5t`) were merged into a single authoritative branch, preserving full git history from both; this file was recovered from the latter and rewritten against the former's more current state; then that authoritative branch (`claude/project-onboarding-status-p35u3v`) fell behind `claude/medusa-repo-clone-ut5dl5` again after a separate session continued pushing to it (drafting and freezing specifications `07`–`11`), and was fast-forward-merged back up to match. Third, 2026-07-19 — this session's own independently-drafted `IMPLEMENTATION_PLANNING.md` (commit `4106b99`) was found to have genuinely diverged from `claude/project-onboarding-status-p35u3v`, which had in the meantime carried Phase 2 forward through Tier A completion and three approved Tier B modules; reconciled via a true three-way merge (`git merge --no-ff`, no rebase/squash/history-rewrite), keeping the more-advanced lineage's current-state documents and prepending a new entry to the two append-only logs (`DECISION_LOG.md`, `CHANGELOG.md`) rather than replacing them. See `DECISION_LOG.md` for all three full entries and the provenance note at the top of this document.
- **Engineering Milestone 13 — Checkout (`07_CHECKOUT_SPECIFICATION.md`) implemented (2026-07-19)**, the seventh and final individual specification in the confirmed order, built on Cart's "Proceed to Checkout" entry point (Milestone 12). Before any UI work, found the environment had **zero shipping options anywhere** (`GET /admin/shipping-options` returned empty) and closed that gap with minimal, **permanent** infrastructure — two Fulfillment Sets on the existing Lagos Warehouse stock location, using Medusa's native `manual_manual` provider: "LiquorCentral Nationwide Delivery" (flat ₦3,000 "Standard Delivery") and "LiquorCentral Pickup" (flat ₦0 "Pickup — Lagos Island"). On top of that: freeform Nigeria-appropriate address capture (§6); the Lagos-eligibility delivery blocking condition (§8/§11, via an honest plain-language `isLagosAddress()` proxy — the real geo-zone definition remains an open business decision, not invented here); fulfillment-group carry-forward into the Order Summary and Order Confirmation, reusing Cart's own grouping utilities unchanged; Pricing Transparency completion (no new code — Cart's own fix already covers this); Payment State Behaviour against the placeholder `pp_system_default` provider (real Paystack integration is still an unbuilt module); Review & Confirm with a restated age-verification reminder; and accessibility fixes (§21/§22 — live-region error announcements, focus-to-heading transitions, a page `<h1>`). **True per-fulfillment-group delivery/slot selection (§9/§10) is deliberately not built** — one combined shipping-method selection serves the whole cart instead, since the `delivery-slot` module (Milestone 4) has no storefront wiring. A live axe-core scan against the first-ever real Shipping Option found and fixed a critical, newly-surfaced accessibility bug in the **shared** `Radio` component (a nested `<button role="radio" aria-checked="true">`, hardcoded regardless of the real `checked` state, inside every consumer's own semantic wrapper). A recurrence of Milestone 11's documented Next.js dev-mode stale-fetch-cache bug was hit again and fixed the same way. A real completed test order's inventory reservation blocked hard-deletion of a QA product; resolved by unpublishing (`status: "draft"`) rather than force-deleting, preserving order history per `08_CUSTOMER_ACCOUNT_SPECIFICATION.md`'s own "permanent record" principle. Full detail in `docs/DECISION_LOG.md` and `storefront/README.md`.

- **Engineering Milestone 14 — Customer Account (`08_CUSTOMER_ACCOUNT_SPECIFICATION.md`) implemented (2026-07-20)**, the eighth specification in the confirmed order, built on Checkout's order-confirmation entry point (Milestone 13) and the vendored template's existing account module — which turned out to be partly stubbed and partly missing relative to the frozen specification. Implemented: password reset against native Medusa Auth (§9, entirely new); a real password-change flow with current-password re-verification (§11 — the vendored version was a non-functional `console.info` stub); a freeform Saved Addresses book matching Checkout's own field structure, with one-address default marking pre-selected at checkout (§12); order history with real status badges and a re-validating Reorder action that clamps to genuine available stock (§13–§15); an honest Notification Preferences stub (§16 — no channel/provider module exists yet); and Privacy & Security with real data visibility plus a real, password-gated request-intake account deletion/deactivation pathway (§17, §18). Investigating email-change support surfaced a genuine, confirmed Medusa Auth architecture limitation via direct source inspection of `@medusajs/auth-emailpass` — no code path renames an auth identity's login-email `entity_id` — so email is shown read-only rather than shipping a control that would silently desync a customer's login credential from their displayed email. Live testing found and fixed two genuine, platform-wide bugs: the shared `Input` component had no `id` attribute at all (no text field built from it had ever had a working label association, anywhere on the platform), and Medusa v2's eager, non-null cart address record had been silently defeating six different "does a real address exist" checks across Checkout since Milestone 13 (fixed with one shared `hasRealAddress()` helper). Full detail in `docs/DECISION_LOG.md` and `storefront/README.md`.
- **Engineering Milestone 15 — Food Ordering (`09_FOOD_ORDERING_SPECIFICATION.md`) implemented (2026-07-20)**, the ninth specification in the confirmed order, built on Customer Account (Milestone 14). Research first found the specification roughly 60–70% already satisfied by prior milestones' work — the genuinely new scope narrowed to two mechanisms the specification names as a decided minimum with no existing backend home, per `TIER_B_FOOD_ATTRIBUTES_MODULE.md`'s own explicit exclusion of both from `food-details`: a binary Available/Unavailable dish flag (§6, §16 — `product.metadata.food_available`, a native field with a new admin toggle widget, deliberately not added to `food-details` itself) and the cook-to-order status progression (§7 — `order.metadata.food_order_status`, a new admin dropdown widget, displayed with real heading/list semantics and a live region across order confirmation, order details, and account order history, its third stage chosen automatically from pickup vs. delivery). Also replaced the three static `/food-central` route pages' unconditional "not taking orders yet" placeholder with a real menu listing, extending the homepage Food Central Spotlight's already-proven query pattern. **The "Available to schedule" third availability state, the same-day cutoff countdown, and any scheduling UI are deliberately not built** — all depend on delivery-slot storefront wiring and cutoff timing still explicitly "not yet built"/"not yet decided" per §25, the same gap Checkout (Milestone 13) already deferred true per-fulfillment-group slot selection on. A live axe-core scan found one pre-existing, platform-wide color-contrast issue traced to the vendored `@medusajs/ui-preset` design-token layer (not a first-party component) — reconfirmed, not fixed, per the same precedent as the identical `Button` finding carried since Milestone 7. Full detail in `docs/DECISION_LOG.md` and `storefront/README.md`.

- **Engineering Milestone 16 — Delivery Tracking (`10_DELIVERY_SPECIFICATION.md`) implemented (2026-07-20)**, the tenth specification in the confirmed order. `10_DELIVERY_SPECIFICATION.md` was found already drafted, Approved — Frozen (v1.0) — correcting a stale note in this document's own prior entry (and in `PROJECT_STATUS.md`) that called it "not yet drafted." Direct comparison against the frozen document found coverage, the two-fulfillment-leg model, pickup, and address handling all already satisfied by prior milestones — the genuinely new work was Wine & Spirits' own delivery-status progression (§10, specified here for the first time: Order Placed → Dispatched → In Transit → Delivered), implemented via the identical native-`order.metadata` + admin-widget pattern Food Ordering already established, plus a `DeliveryStatus` wrapper rendering both catalogs' statuses independently and clearly labeled on a mixed order (§15 — "never merged into one status"). Rider assignment, proof of delivery, delivery fees, rescheduling/cancellation policy, and proactive WhatsApp/SMS messaging remain deliberately unbuilt, per the specification's own naming of them as operational/business parameters or dependent on the still-unbuilt notification provider. Full detail in `docs/DECISION_LOG.md` and `storefront/README.md`.

**In Progress**
- None. Milestones 1 through 16 are complete — Backend Foundation, `wine-details`, `food-details`, `delivery-slot`, the storefront scaffold, Phase 0c Storefront Foundation, and all ten specifications in the confirmed order (Navigation, Homepage, Product Listing, Search, Product Details, Cart, Checkout, Customer Account, Food Ordering, Delivery Tracking). The local payment provider and notification provider *modules themselves* remain not yet implemented in code, though the underlying business decisions blocking them are now largely resolved (see below).

**Not Started**
- Every Phase 2 (Implementation Planning) Tier C–F document — per `IMPLEMENTATION_READINESS_REPORT.md` (v1.0, Approved), no Tier C document is required before implementation begins, and none is currently planned as a precondition to it.
- Food Ordering (`09_FOOD_ORDERING_SPECIFICATION.md`) — the confirmed next milestone, followed by Delivery Tracking.
- The local payment provider and notification provider *modules* — the provider choice (Paystack) and notification channels (Email/WhatsApp/in-app) are now decided, but neither module has been implemented in code yet; the notification provider's Tier B architecture document also still needs to move from Draft to Approved before its implementation milestone. Checkout and Customer Account (Milestones 13, 14) were both built against the placeholder `pp_system_default` provider, same as every milestone before them.
- A real customer-facing email-change flow (§11) — blocked on a confirmed Medusa Auth architecture limitation (no mechanism to rename a login identity's email), not merely undecided; needs new backend work.
- Account deletion/deactivation *enforcement* — a real request-intake mechanism exists (Milestone 14), but the underlying policy (§18) remains an open business/legal decision, so nothing is automated against it yet.
- True per-fulfillment-group delivery-method/slot selection in Checkout (§9/§10) — the `delivery-slot` module (Milestone 4) has no storefront wiring yet; one combined shipping-method selection serves the whole cart today.
- Any custom API route, real product catalog data, and the Meilisearch/Sanity integrations (both still formally unapproved).
- Storybook/component preview — deliberately deferred since Milestone 6 as not yet warranted by the current component count.

**Blocked / Awaiting Paul's Approval**
- This list is **no longer independently maintained here** — it drifted out of sync with `docs/PROJECT_STATUS.md`'s own "Blockers" and "Decisions awaiting Paul's approval" sections across Milestones 7–10 (both documents describing the same decisions in slightly different, occasionally contradictory wording). `docs/PROJECT_STATUS.md`'s versions are current as of Milestone 14 and use an explicit ~~strikethrough~~-with-resolution-date convention as items get resolved — **treat those two sections as authoritative** rather than this document's own copy.
- Headline state as of Milestone 15, for a reader who wants it without following the cross-reference: **payment provider (Paystack), notification channels, launch delivery areas (Lagos-wide for Wine & Spirits, Lagos Island for Food Central), delivery pricing model (distance-based, ~₦3,000 starting), operating hours (9am–11pm), inventory display policy, age-verification MVP mechanism (a checkbox), cash-on-delivery (not supported), and guest-checkout terms are all now decided.** Still genuinely open: the wine-/food-attribute field lists and allergen-accuracy ownership (safety-critical), the notification provider's Tier B document (still Draft), Wine & Spirits' delivery *mechanism* (fleet vs. courier), the exact Nigerian VAT rate, the alcohol return/refund policy, delivery-slot operational parameters (slot length/cutoff/capacity), the precise Lagos geo-zone definition (Checkout's own delivery-eligibility blocking condition uses a plain-language proxy pending this), whether a hard age-recheck happens at order confirmation, Meilisearch/Sanity formal sign-off, NDPR/data-retention specifics, Cart's own Saved-for-Later item (still needs backend scoping), Customer Account's own two newly-flagged items (the exact account deletion-vs-deactivation policy — a real request-intake mechanism exists, the policy behind it doesn't; and whether to prioritize the backend work a real email-change flow would need), and Food Ordering's own three newly-flagged items (whether the already-decided 9am–11pm figure specifically resolves Food Central's own kitchen-operating-hours question, distinct from general delivery-slot timing, per `TIER_B_FOOD_ATTRIBUTES_MODULE.md`/`TIER_B_DELIVERY_SLOT_MODULE.md`'s shared confirmation that neither module owns that state; the exact sub-granularity beneath the now-decided four-stage cook-to-order status progression; and the "Available to schedule" third availability state, which depends on the still-unbuilt delivery-slot storefront wiring).

---

## 9. Current Roadmap

Full detail in `ROADMAP.md`. Summary:

- **Phase 0 — Brand & Design Foundation: ✅ Complete.** Includes Phase 0b (Design System Foundations, frozen as v2.0).
- **Phase 0c — Storefront Foundation: ✅ Complete (Engineering Milestone 6, 2026-07-19).** Reusable routing/layout/provider/design-token/UI-primitive/accessibility/testing infrastructure is in place in `storefront/`; see `storefront/README.md`. All ten specifications in the confirmed order now sit on top of it — Navigation (Milestone 7), Homepage (Milestone 8), Product Listing (Milestone 9), Search (Milestone 10), Product Details (Milestone 11), Cart (Milestone 12), Checkout (Milestone 13), Customer Account (Milestone 14), Food Ordering (Milestone 15), and Delivery Tracking (Milestone 16) — all ✅ Complete. Admin Workflows (`11_ADMIN_WORKFLOWS_SPECIFICATION.md`, already Approved — Frozen) is next.
- **Phase 0d — Product Specifications: ✅ Complete.** All 11 specifications Approved — Frozen at v1.0.
- **Phase 0e — Documentation governance and repository reconciliation: ✅ Complete** (three times — see `DECISION_LOG.md`).
- **Phase 0f — Implementation Planning (Paul's "Phase 2"): ✅ Complete.** `IMPLEMENTATION_PLANNING.md` v1.0 defines the six-tier structure; `TIER_A_FOUNDATIONAL_RECONCILIATION.md` (v1.5), `TIER_B_PRODUCT_RELATIONSHIP_MODULE.md`, `TIER_B_WINE_ATTRIBUTES_MODULE.md`, `TIER_B_FOOD_ATTRIBUTES_MODULE.md`, `TIER_B_DELIVERY_SLOT_MODULE.md`, and `TIER_B_LOCAL_PAYMENT_PROVIDER_MODULE.md` are all Approved at v1.0; `MODULE_INVENTORY.md` is live; `IMPLEMENTATION_READINESS_REPORT.md` (v1.0, Approved) concludes no Tier C document is required before implementation begins. Implementation is underway, proceeding autonomously per Paul's Milestone-4 authorization (Section 1) rather than a per-milestone approval gate.
- **Phase 1 — Foundation (backend):** Stand up Medusa on Postgres + Redis — **✅ complete (Engineering Milestone 1)**; storefront scaffold — **✅ complete (Engineering Milestone 5)**; storefront foundation/design-token layer — **✅ complete (Engineering Milestone 6)**, see `backend/README.md` and `storefront/README.md`. Remaining: connect a real payment provider (Paystack is now the approved choice, `DECISION_LOG.md`, but the module itself is not yet implemented). Wine & Spirits' full end-to-end buy flow — browse, search, the product detail page, cart, and checkout — is now complete (Milestones 7–13), built against the placeholder `pp_system_default` provider pending real Paystack integration.
- **Phase 2 — Product data foundation:** wine-attributes module — **✅ complete (Engineering Milestone 2, 2026-07-19)**, see `backend/apps/backend/src/modules/wine-details/README.md`. *(Note: `ROADMAP.md`'s own numbered "Phase 2" is a different, backend-track phase than Paul's "Phase 2 — Implementation Planning" above — see `ROADMAP.md`'s naming note.)*
- **Phase 3 — Food Central catalog and delivery foundation:** food-attributes module — **✅ complete (Engineering Milestone 3, 2026-07-19)**, see `backend/apps/backend/src/modules/food-details/README.md`; Lagos-scoped fulfillment and ordinary (non-scheduled) ordering/pickup remain not started.
- **Phase 4 — Delivery scheduling:** delivery-slot module — **✅ complete (Engineering Milestone 4, 2026-07-19)**, see `backend/apps/backend/src/modules/delivery-slot/README.md`.
- **Phase 5 — Delivery communication:** notification provider (WhatsApp/SMS) integration — Tier B architecture still Draft, not Approved.
- **Phase 6 — Search:** Meilisearch integration.
- **Phase 7 — Content and CMS:** Sanity integration.
- **Phase 8 — Mobile app:** after the web storefront's data layer and checkout are proven.
- **Phase 9 — Operational hardening.**

**Explicitly out of scope** (do not schedule without a new decision): any marketplace/vendor functionality, loyalty/subscription mechanics, third-party carrier integration.

---

## 10. Immediate Next Step

Planning (Phases 0–2) is complete. Engineering proceeds **autonomously**, per Paul's explicit authorization (see Section 1), now a durable standing rule (not a one-time grant, confirmed again in the batch business-decision entry below): **Milestones 1 through 16 are all complete** (2026-07-20) — Backend Foundation, `wine-details`, `food-details`, `delivery-slot`, the storefront scaffold, Phase 0c Storefront Foundation, and all ten specifications in the confirmed order (Navigation, Homepage, Product Listing, Search, Product Details, Cart, Checkout, Customer Account, Food Ordering, Delivery Tracking) — see `backend/README.md`, `storefront/README.md`, each module's own README, and `DECISION_LOG.md` for full detail. **A project-wide requirement took effect during Milestone 8 and remains in force: every milestone changing customer-facing or admin UI must include real browser screenshots of every significant state in its completion report, not just automated-check output.** This has caught genuine bugs no automated check found in several milestones since (a mega-menu layout bug and an RSC-boundary constant bug in Milestone 8; a `fields`-parameter override bug in Milestone 9), and Milestone 11's, 12's, 13's, 14's, 15's, and 16's own real axe-core scans (not static review) each found and fixed (or, where genuinely out of scope, reconfirmed and knowingly left) genuine accessibility violations (Section 8 has full detail for each) — Milestone 14's own testing additionally found two genuine, platform-wide *functional* bugs (not just accessibility) via live end-to-end testing: the shared `Input` component's missing label association, and Checkout's six broken "does a real address exist" checks against Medusa's eagerly-created cart address record. The next unblocked step, per the confirmed specification order, is Admin Workflows (`11_ADMIN_WORKFLOWS_SPECIFICATION.md`) — already Approved — Frozen, so implementation can proceed directly against it.

1. **The confirmed specification implementation order is: Navigation → Homepage → Search → Product Listing → Product Details → Cart → Checkout → Customer Account → Food Ordering → Delivery Tracking.** Product Listing was built slightly ahead of Search in that nominal sequence (no dependency either way, not a conflict) — otherwise every milestone has followed the order as directed. All eight specifications through Customer Account are now complete. **Food Ordering (`09_FOOD_ORDERING_SPECIFICATION.md`) is the confirmed next milestone**, and is genuinely unblocked — no new business decision gates it.
2. **A large batch of previously-open business decisions was supplied after Milestone 9** (payment provider, notification channels, launch delivery areas/pricing, operating hours, inventory policy, unified search presentation, MVP age-verification mechanism, guest-checkout terms, tax/currency display, coupon capability, loyalty/wishlist scope — full detail in `DECISION_LOG.md`), together with the explicit standing "keep building without stopping for approval" rule. `docs/PROJECT_STATUS.md`'s "Blockers" and "Decisions awaiting Paul's approval" sections are the **authoritative, current inventory** of what's resolved vs. still open — this document no longer independently maintains that list (see Section 8's "Blocked / Awaiting Paul's Approval" note, which explains why and points there).
3. **Still genuinely open, most urgently:** the wine-/food-attribute field lists and allergen-accuracy ownership (safety-critical, blocks full catalog population); the notification provider's Tier B document (still Draft — channels are decided, the module's own architecture is not); Wine & Spirits' delivery *mechanism* (fleet vs. courier — the delivery *area* is decided); the exact Nigerian VAT rate; the alcohol return/refund policy; delivery-slot operational parameters (slot length, cutoff, capacity — also now blocking Checkout's own true per-fulfillment-group delivery/slot-selection UI); the precise Lagos geo-zone definition (Checkout's delivery-eligibility blocking condition uses a plain-language proxy pending this); whether a hard age-recheck happens at order confirmation (Checkout only restates the existing reminder); Meilisearch/Sanity formal sign-off (Search's own unified-list presentation is built and doesn't need either; Meilisearch-backed typo tolerance, synonyms, autocomplete, and facets remain blocked on it); and Cart's Saved-for-Later item (still needs backend scoping — its sibling item, whether Checkout carries the cart's two-fulfillment-group distinction forward, is now resolved: it does).
4. **Two architectural questions from `TIER_B_DELIVERY_SLOT_MODULE.md` remain open**: the pickup-slot mechanism boundary (§5, §19), and the kitchen-capacity-vs-rider-capacity reconciliation question (§6, §18).
5. **A Design-System-level accessibility finding, first surfaced in Milestone 7, reconfirmed present in Milestones 8, 11, 12, and 13, not this project's engineering to resolve unilaterally**: the shared `Button`/`text-ui-fg-interactive` color tokens measure below the WCAG AA 4.5:1 requirement — confirmed systemic (Cart page, Homepage's Hero/Spotlight CTAs, the Product Details add-to-cart button, the Cart's own "Go to checkout"/discount-code link, and Checkout's own edit-address/add-discount links) via real axe-core testing each time, deliberately left unaltered since it requires a Design-System-level decision (a color or text-weight change to a Frozen document), not a per-page fix.
6. **A mobile accordion footer was never built** (`01_NAVIGATION_SPECIFICATION.md` §8.9) — Milestone 7's footer is static/always-expanded on every viewport, flagged in Milestone 8 but not fixed since it's Navigation's own component.
7. **"Featured"/"Relevance" sorting (Product Listing, Search) is honestly a no-op today**, not a real merchandising-controlled order — no per-product manual-rank field exists in Medusa's native model, and none was invented. Needs the attribute-module field-list approvals (item 3) before it can become real.
8. **Pairing Recommendations ("pairs with")** remain unscoped in `MEDUSA_EXTENSIONS.md` — six specifications now depend on this same gap (Homepage, Navigation, Search, Product Listing, Product Details, Cart), the strongest signal yet that it should be treated as a genuine priority to scope, not a recurring footnote.
9. **Gift Wrap (Product Details §16, Cart §15) is built and now interoperable across both surfaces** — a metadata-linked (`gift_wrap_for`) cart line item recognized identically whether added at the PDP's add-to-cart moment or via the cart's own per-item toggle; it renders only when a real product with handle `gift-wrap` exists in the catalog.
10. **This project has still never seeded a single real catalog product persistently.** Every UI milestone since Milestone 9 has validated against temporary, clearly-marked QA-only products created and fully deleted via the Admin API. Milestone 14's own environment started from a completely fresh database (zero products) — its own temporary QA food product was fully deleted, and its QA wine product was unpublished (`status: "draft"`, not deleted) after real placed-order (and reorder) inventory reservations blocked hard-deletion — the same pattern Milestone 13 established.
11. **Checkout (Milestone 13) closed a genuine, total shipping-infrastructure gap with a new, permanent seed** — this environment had zero Fulfillment Sets/Service Zones/Shipping Options anywhere before that milestone. Two Fulfillment Sets now exist on the Lagos Warehouse stock location using Medusa's native `manual_manual` provider: "LiquorCentral Nationwide Delivery" (flat ₦3,000 Standard Delivery) and "LiquorCentral Pickup" (flat ₦0 Pickup — Lagos Island). This is production configuration, not QA data — it was not removed at the end of that milestone. See `DECISION_LOG.md`'s Milestone 13 entry for exact IDs.
12. **True per-fulfillment-group delivery/slot selection in Checkout (§9/§10) is deliberately not built** — Medusa's shipping-method-per-cart model is cart-wide, and the `delivery-slot` module (Milestone 4) has no storefront wiring. One combined shipping-method selection serves the whole cart today; this is the same delivery-slot-operational-parameters gap named in item 3.
13. **Customer Account (Milestone 14) found and fixed a real, first-render-visible correctness bug spanning six call sites across five Checkout files**, not just its own new code — every `cart.shipping_address &&` truthy check (added across Milestone 13) had been evaluating `true` from the moment a cart is created, since Medusa v2 creates the address record eagerly and non-null. Fixed with one shared `hasRealAddress()` helper (`lib/util/cart-fulfillment.ts`), reused everywhere the same gotcha appeared. A reader touching checkout code should reach for this helper, not bare `cart.shipping_address` truthiness, for any future "has a real address been entered" check.
14. **A customer cannot currently change the email address they log in with.** This is a confirmed backend architecture limitation (native Medusa Auth's `emailpass` provider has no mechanism to rename a login identity), not a missing UI — do not attempt to "fix" this by wiring up `sdk.store.customer.update({ email })` alone; it silently desyncs the Customer record from the actual login credential. Needs new backend work before it can be built.

No Tier C–F document begins — none was found necessary. No further Product Specification work is needed (all 11 are frozen and modifiable only in response to a new business decision). Engineering milestones proceed automatically per the standing authorization — the next unblocked step is Food Ordering (`09_FOOD_ORDERING_SPECIFICATION.md`), absent new direction from Paul.

---

## 11. Rules for Future AI Sessions

Every future AI assistant working on this project must:

1. **Read `docs/AI_HANDOFF.md` first** (this document), then `docs/README.md`, then `docs/PROJECT_STATUS.md` — see Section 6 for the full reading order.
2. **Consult the relevant documentation** in `/docs` before answering questions or making changes, rather than guessing.
3. **Treat the documentation as the single source of truth** — not memory, not assumption, not inference from the codebase alone.
4. **Never rely on previous chat history.** Assume no prior conversation exists or is accessible. If something a user describes as "already created" or "already decided" doesn't appear in `/docs` on the current branch, check `git log --all` / `git branch -r` before concluding it doesn't exist — it may be sitting on an unmerged branch, exactly as happened with this file (see the provenance note at the top of this document and `DECISION_LOG.md`). Do not fabricate content to fill the gap either way, and do not declare work "lost" without first checking every branch.
5. **Never contradict approved business decisions** (Section 3) or architecture decisions (Section 4).
6. **Never modify the four frozen Phase 0 documents** (`PRODUCT_BLUEPRINT.md`, `BRAND_IDENTITY.md`, `EXPERIENCE_PRINCIPLES.md`, `DESIGN_SYSTEM.md`) unless Paul explicitly makes a business decision that requires it.
7. **Never introduce marketplace concepts** — multi-vendor support, seller onboarding, vendor modules, commission/payout logic — unless Paul explicitly changes the business model. If asked to build something that resembles a marketplace feature, stop and confirm with Paul rather than proceeding.
8. **Never invent business or brand decisions.** Where a document has a placeholder or an open question (Section 8), leave it open and flag it for Paul rather than assuming an answer.
9. **Never draft a placeholder specification without Paul's explicit direction to begin that specific file** — the placeholder-only structure in `/docs/specifications/` is deliberate.
10. **Log every material decision** in `docs/DECISION_LOG.md` (append, don't edit old entries) and keep `docs/PROJECT_STATUS.md` current in the same change.
11. **When reconciling or merging documentation branches, merge — do not recreate.** Preserve git history, prefer the more current source for genuinely conflicting content, and only edit a document when reconciliation is actually required (a real inconsistency), not because a second version of it exists somewhere.
12. **Any milestone changing customer-facing or admin UI must include real browser screenshots of every significant state in its completion report** (a standing requirement since Milestone 8, not a one-time ask) — desktop/mobile/tablet where behavior differs, empty/loading/error/success states, and interactive states (open/closed, expanded/collapsed, hover/focus) where meaningful. Real running-application screenshots only, never mockups or generated images. Do not consider such a milestone complete on `tsc`/lint/Jest/axe-core passing alone — Milestone 8 found two genuine bugs (a mega-menu layout bug; a React-Server-Components cross-boundary constant bug) that every one of those checks missed, and only a real screenshot/end-to-end pass caught.
13. **Continue building autonomously, milestone by milestone, without stopping for approval** (a standing rule since 2026-07-19, see `DECISION_LOG.md`) — the default is to keep implementing the confirmed specification order (Navigation → Homepage → Search → Product Listing → Product Details → Cart → Checkout → Customer Account → Food Ordering → Delivery Tracking, unless objective architectural evidence says otherwise). Stop only for a genuine business decision, a genuine architectural conflict, or a contradiction with an Approved document — not for ordinary engineering uncertainty resolvable from Approved documentation, repository history, Medusa's own documentation, or framework convention.
14. **Never stop for missing API keys, credentials, or production secrets.** Implement against environment variables, leave clearly-labeled placeholders, validate everything validatable without live credentials (build/type-check/lint/tests/structure), and document in the completion report exactly what could not be validated because credentials were unavailable. This applies now specifically to Paystack (payment) and WhatsApp Business API (notifications) — both approved providers with no credentials supplied yet.

---

## 12. Project Principles

- **Premium over flashy.** Restraint and quality signal trust; novelty for its own sake does not.
- **Simplicity over feature bloat.** Ship what customers need to buy with confidence, not everything that's technically possible.
- **Documentation before implementation.** Decisions get written down and approved before code gets written.
- **Extend Medusa, never modify its core.** Custom logic lives in modules, module links, workflow hooks, and admin widgets/routes — never in edits to the `medusa/` submodule.
- **Research principles, never copy competitors.** Design and product decisions are grounded in what works for customers, not in mimicking other retailers.
- **Every page should reduce friction and increase purchase confidence.** This is the standard every screen and flow is judged against.
- **Mobile-first**, with a stricter performance bar for Food Central specifically (more likely to be a fast, one-thumb, on-the-go action).
- **Accessibility by default** — contrast, keyboard navigation, alt text, focus states — built in from launch, not retrofitted.
- **Performance matters.** Page weight and interaction latency are treated as conversion metrics, not just engineering ones.

---

## 13. Versioning

| Field | Value |
|---|---|
| Document Version | 5.4 |
| Last Updated | 2026-07-19 |
| Project Phase | Phase 0 complete and frozen; Phase 1 — Product Specifications complete (all 11 frozen); Phase 2 — Implementation Planning complete and formally concluded; **Engineering — Milestones 1–12 complete (Backend Foundation, `wine-details`, `food-details`, `delivery-slot`, storefront scaffold, Phase 0c Storefront Foundation, Navigation, Homepage, Product Listing, Search, Product Details, Cart); payment provider (Paystack), notification channels, delivery areas/pricing, operating hours, and several other business decisions approved 2026-07-19 — see `DECISION_LOG.md`; engineering now proceeds continuously without stopping for per-milestone approval** |
| Next Planned Milestone | Food Ordering (`09_FOOD_ORDERING_SPECIFICATION.md`) — confirmed next per the specification implementation order, genuinely unblocked — see Section 10 |
