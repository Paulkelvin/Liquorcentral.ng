# Project Status

**Status:** Approved (living, always current)
**Version:** 3.9
**Owner:** Program
**Last Updated:** 2026-07-19

**This document must always be current.** If you complete work, resolve a question, or hit a blocker, update this file in the same change — treat an out-of-date status as a bug.

## Current phase

**Phase 0 (Brand & Design Foundation) is complete and frozen. Phase 1 — Product Specifications is complete. Phase 2 — Implementation Planning is underway: Tier A (Foundational Reconciliation) is complete and Approved (v1.2); two Tier B documents — the Product Relationship Module and the Wine & Spirits Attributes Module — are Approved at v1.0; a third, the Food Attributes Module, is drafted at v1.0 — Draft, awaiting Paul's review.** A living `MODULE_INVENTORY.md` now indexes every module identified across the project. No Tier C–F document has been drafted; Phase 0c component specification and Phase 1 backend foundation (per `ROADMAP.md`) also still await Paul's explicit direction to begin.

`PRODUCT_BLUEPRINT.md`, `BRAND_IDENTITY.md`, `EXPERIENCE_PRINCIPLES.md`, and `DESIGN_SYSTEM.md` v2.0 are approved and now constitute the authoritative foundation of the project. **Per Paul's explicit instruction, none of these four documents are to be modified unless a business decision changes** — they are inputs to everything that follows, not living drafts.

The project has moved through: (1) initial Medusa architecture research, (2) technology/marketplace research (since partially superseded — see below), (3) UX and product research, (4) establishing `/docs` as the project's permanent, versioned source of truth, (5) drafting the Brand Identity foundation, (6) drafting the Experience Principles that define how customers should experience the product, (7) Paul finalizing the positioning statement/category definition, (8) Paul approving `BRAND_IDENTITY.md` v1 and `EXPERIENCE_PRINCIPLES.md` v1.0 in full, (9) drafting `DESIGN_SYSTEM.md` Part B (Foundations), (10) refining §B6 into an explicit three-tier Color Architecture per Paul's feedback, (11) Paul approving the Color Architecture's WCAG validation and requesting one final refinement, after which `DESIGN_SYSTEM.md` was frozen as Version 2.0, and **(12) Paul declaring Phase 0 complete and opening Phase 1 — Product Specifications**, a new `/docs/specifications/` directory of behavior-level specifications (what each product surface must do, not what it looks like) built on top of the four frozen documents. No implementation code, UI design, wireframes, or mockups have been produced at any point so far — this has been, deliberately, research and definition work only.

**Phase 1 status: complete.** `/docs/specifications/` created with 11 numbered specification files, and **all 11 — `01_NAVIGATION_SPECIFICATION.md` through `11_ADMIN_WORKFLOWS_SPECIFICATION.md` — are now Approved — Frozen at v1.0** (2026-07-18). Each is the authoritative reference for its domain; per `DOCUMENTATION_GOVERNANCE.md` Section 5, each may only be modified in response to an explicit new business decision from Paul. No specification remains in progress or as a placeholder.

**Repository note (2026-07-18):** this session's working branch (`claude/medusa-repo-clone-ut5dl5`) was found to have diverged from a second, more current session branch (`claude/project-onboarding-status-p35u3v`, which already carried the reconciliation, governance hardening, and specifications 01–06 through freeze). No PR existed for either branch. The two were reconciled via a clean fast-forward merge (no history rewritten, nothing squashed) and pushed; this session now continues directly from that state. See `DECISION_LOG.md` for the full entry.

**Repository note, round three (2026-07-18):** a third session, working on `claude/project-onboarding-status-p35u3v` directly, found that branch had fallen behind `claude/medusa-repo-clone-ut5dl5` (which had, in the interim, completed specifications `07`–`11` through freeze — the branch's own uncommitted local draft of `07_CHECKOUT_SPECIFICATION.md` was superseded and stashed rather than committed). Confirmed as a pure fast-forward (`claude/project-onboarding-status-p35u3v`'s HEAD was a strict git ancestor of `claude/medusa-repo-clone-ut5dl5`'s tip — no divergent content, no conflict) before merging and pushing. `AI_HANDOFF.md` was found stale (still describing Phase 1 as underway with only the homepage drafted) and corrected in the same change. See `DECISION_LOG.md` for the full entry.

## Completed work

- Medusa v2.17.2 vendored into this repository as a git submodule (`./medusa`).
- Full architecture research of the vendored Medusa codebase (monorepo structure, commerce modules, workflow engine, module-link extension pattern, admin dashboard extensibility) — distilled into `ARCHITECTURE.md`.
- Technology recommendations researched for storefront, search, CMS, and authentication — distilled into `TECH_STACK.md` and `MEDUSA_EXTENSIONS.md`. (An earlier phase of this research also explored a multi-vendor marketplace architecture; that architecture is **retired** — see the no-marketplace decision below.)
- UX and product research across premium commerce, wine retail, food-ordering, and Nigerian-market conventions — distilled into `PRODUCT_BLUEPRINT.md`, `USER_FLOWS.md`, and `DELIVERY_MODEL.md`.
- Business model finalized by Paul: single-company retailer, no marketplace, no vendors (see `DECISION_LOG.md`).
- `PRODUCT_BLUEPRINT.md` v1 drafted, covering all 18 required sections with reasoning, business benefit, and Medusa impact for each.
- Full `/docs` documentation system established (see `README.md` for the index).
- **`BRAND_IDENTITY.md` v1 drafted** — vision, mission, values, personality, voice/tone, emotional and perception goals, brand story, positioning, value proposition, and visual/color/typography/photography/motion/iconography/accessibility principles for the four already-approved brand colors (Primary Red `#EC2D07`, Green `#1A9902`, Gold `#CFCA43`, Off White `#F3F5F0`). Includes a computed WCAG contrast analysis of all four colors — notably, **gold fails contrast against the off-white base at every text size** and should be reserved for dark-ground/accent use only (see `BRAND_IDENTITY.md` §13).
- `BRAND_GUIDELINES.md` reconciled with `BRAND_IDENTITY.md` — the two now have distinct, non-overlapping responsibilities (identity/strategy vs. tactical asset execution); see either document's "Relationship" section.
- **`EXPERIENCE_PRINCIPLES.md` v1.0 drafted** — 15 experience principles (Confidence Before Complexity, Simplicity Before Features, Premium Through Discipline, Photography Sells First, Guide Without Intimidating, Speed Builds Trust, and more) plus a product vision, competitive positioning ("Premium Lifestyle Commerce Platform"), and a single success-metric question every design decision should be checked against. Explicitly complements, not duplicates, `BRAND_IDENTITY.md` — see its own "Relationship to other documents" section.
- **Positioning finalized by Paul:** the Positioning Statement (`BRAND_IDENTITY.md` §10) and Category Definition (`EXPERIENCE_PRINCIPLES.md`) were both rewritten in Paul's own words and cross-referenced as consistent — the one open reconciliation item between the two documents is resolved.
- **Document status convention adopted** — every file in `/docs` now carries a `Status | Version | Owner | Last Updated` header (see `README.md` → "Document status convention"), so anyone can tell at a glance which documents are settled vs. still being shaped.
- **`BRAND_IDENTITY.md` v1 and `EXPERIENCE_PRINCIPLES.md` v1.0 approved in full by Paul (2026-07-18)** — the Phase 0 gate is lifted; see `DECISION_LOG.md`.
- **`DESIGN_SYSTEM.md` restructured into Part A (Principles) and Part B (Foundations)** — concrete typography scale, spacing scale, grid, elevation/shadows, border radius, motion timing, breakpoints, icon sizing, form behaviors, and accessibility tokens, per Paul's Design System Foundations recommendation.
- **§B6 Color Architecture built as an explicit three-tier structure:** Tier 1 (Brand Colors, fixed/unchanged), Tier 2 (Functional UI Colors — Success reuses Green intentionally; Warning `#B45309`, Danger `#B3261E`, and Information `#2B6CB0` are new, independently-chosen, WCAG-AA-passing colors, deliberately distinct from Gold and from Primary Red respectively), and Tier 3 (Semantic Design Tokens). Gold's role is explicitly restricted to premium/curation use, never a functional state. A full Neutral System (7 steps, all text-bearing steps pass WCAG AA) covers text/borders/dividers/disabled/surfaces/overlays.
- **`DESIGN_SYSTEM.md` v2.0 — final refinement and freeze, approved in full by Paul (2026-07-18):** Tier 3 tokens reorganized around semantic intent as the system's canonical language (Primary, Secondary, Accent, Surface, Surface Elevated, Text Primary, Text Secondary, Border, Divider, Focus, Interactive/Hover/Active, Disabled, Success, Warning, Danger, Information) rather than literal/functional naming; a new **Interactive States** mechanism (hover/active as percentage overlays on whichever base color is in use, not fixed per-color hex values); a new **Surface Elevated** token (`#FFFFFF`) for cards/modals/dropdowns; a new **Future Theme Support** section documenting how the token architecture enables dark mode/seasonal themes/brand refreshes/accessibility themes without touching component code; a new **Component Philosophy** section; and a concluding **Design Quality Checklist** every future component must satisfy. `DESIGN_SYSTEM.md` is now frozen as **Version 2.0 — the authoritative Design System foundation** for all future UI and component work. See `DECISION_LOG.md`.
- **Phase 0 declared complete by Paul (2026-07-18); Phase 1 — Product Specifications opened.** `/docs/specifications/` created with 11 numbered files. `02_HOMEPAGE_SPECIFICATION.md` fully drafted (v0.1) covering all 25 required sections — homepage responsibilities, business/customer goals, primary journeys, information hierarchy, all 9 homepage sections each broken down into Purpose/Business rationale/Customer value/Behaviour/Mobile/Desktop/Accessibility/Backend dependencies/Future extensibility, backend data requirements, search/discovery entry point, Food Central integration strategy, trust and delivery messaging, SEO/accessibility/performance targets, analytics events, empty/loading/error states, Version 1 scope, future enhancements, risks/assumptions, and acceptance criteria. Explicitly rules out an auto-rotating hero carousel, grounded in cited usability/performance research. The remaining 10 specifications (`01`, `03`–`11`) were created as approved placeholders only (Document Purpose, Scope, Dependencies, Planned Sections, Status = Not Started) — their detailed content was deliberately not invented ahead of sequencing.
- **Repository reconciliation (2026-07-18):** an audit found this documentation set (this branch, `claude/medusa-repo-clone-ut5dl5`) and a separately-authored `docs/AI_HANDOFF.md` (on branch `claude/ai-handoff-docs-ufdn5t`) had diverged from a common commit and were never merged — each branch's tooling was unaware of the other's existence, and this document previously (incorrectly) reported the `AI_HANDOFF.md` work as lost. Both branches were merged into one authoritative working branch, preserving full history from both; `docs/AI_HANDOFF.md` was recovered and rewritten to reflect the current, reconciled state. `docs/PRODUCT_BLUEPRINT.md`'s status header was also corrected from "Draft — pending review" to "Approved — Frozen," matching its actual status as one of the four frozen Phase 0 documents (an inconsistency the audit surfaced). **Approved by Paul, 2026-07-18.** See `DECISION_LOG.md` for the full entry.
- **Documentation governance hardening pass (2026-07-18):** `docs/DOCUMENTATION_GOVERNANCE.md` created as the governing standard for all documentation work — purpose, single source of truth rule, the document authority hierarchy, the full document lifecycle (nine statuses, including two new ones this pass added: Not Started and Deprecated/Archived formalized), change rules (including which documents require Paul's explicit approval before modification), cross-reference rules, versioning rules, AI and human contributor rules, the end-to-end repository workflow, a documentation quality checklist, future-maintenance guidance, and the audit process itself. A full `/docs` audit against that new standard followed, fixing every inconsistency found: `docs/README.md`'s status-convention section was rewritten to defer to the new governance document instead of maintaining its own shorter, now-outdated list of statuses; `docs/README.md` and `docs/AI_HANDOFF.md` were both updated to reference and index the new document, including reading order, document-count, and document-table updates. No broken cross-references, terminology drift, duplicate concepts, or orphaned documents were found in the rest of `/docs`. **Approved by Paul, 2026-07-18.** See `DECISION_LOG.md` for the full entry.
- **`01_NAVIGATION_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: navigation philosophy, business/customer objectives, information architecture, global/desktop/mobile navigation strategy, footer and header behavior, mega menu strategy (Wine & Spirits only, capped link budget, grouped/labeled columns), a data-driven (non-hardcoded) product category and collection navigation model, wine discovery's three-layer system, Food Central's deliberately flat menu-list pattern, search entry points (with search-within-category support and visible, non-muted field prominence), account and cart/checkout navigation rules, breadcrumb strategy, internal linking, deep linking, accessibility and keyboard navigation (mega menu/mobile drawer built as a disclosure pattern, not an ARIA `menu` widget — a deliberate research-grounded choice), responsive behavior tied to `DESIGN_SYSTEM.md` breakpoints, empty/error states, analytics events, SEO and performance considerations (including faceted-URL canonicalization as a named, unassigned risk), a future-expansion strategy naming the category-tree as the scalability mechanism, risks/assumptions, and acceptance criteria. Grounded in limited external UX/accessibility research (Baymard, NN/g, WAI-ARIA APG, luxury-ecommerce and food-delivery navigation research) cited inline and in a Sources section — no layouts, menu wording, or proprietary interactions copied from any source. Flags the same "pairs with" backend-relationship gap `02_HOMEPAGE_SPECIFICATION.md` §24 already flagged, now noted as a dependency in three navigation surfaces rather than one.
- **`01_NAVIGATION_SPECIFICATION.md` approved by Paul as overall architecture, then finalized to v1.0 with a refinement pass (2026-07-18):** Paul confirmed the specification aligns with `PRODUCT_BLUEPRINT.md`, `BRAND_IDENTITY.md`, `EXPERIENCE_PRINCIPLES.md`, and `DESIGN_SYSTEM.md`, then requested five additions before freezing v1.0: **Navigation Governance** (a table classifying every navigation element as data-driven/merchandising-controlled, marketing-controlled, or developer-required, organized around the rule that structure is developer-governed and content is data-governed); **Merchandising Strategy** (promotional Collections — featured, seasonal, limited-time, gifting, editorial, new-arrivals, premium — as an optional, capped, auto-expiring layer distinct from permanent taxonomy, with an explicit no-fake-urgency rule per `EXPERIENCE_PRINCIPLES.md` #15); an expanded **Scalability** treatment in §28 (a table demonstrating the architecture's reach into services, subscriptions, educational content, events, corporate gifting, and a hypothetical future business line, none of it authorized or committed work); expanded **Navigation Performance** expectations in §27 (a maximum three-level navigation depth budget, interaction-latency targets reusing `DESIGN_SYSTEM.md`'s existing motion tokens, and a tiered graceful-degradation model); and a closing **Navigation Quality Checklist** every future navigation change must satisfy, mirroring `DESIGN_SYSTEM.md`'s own Design Quality Checklist. Version bumped to 1.0 and status changed to **Under Review**. See `DECISION_LOG.md` for the full entry.
- **`01_NAVIGATION_SPECIFICATION.md` v1.0 approved and frozen by Paul (2026-07-18).** Status changed to **Approved — Frozen**; it is now the authoritative reference for all navigation implementation and may only be modified in response to a new business decision, per `DOCUMENTATION_GOVERNANCE.md` Section 5. See `DECISION_LOG.md`.
- **`03_SEARCH_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: search philosophy, business/customer objectives, search entry points (integrating with, not redefining, `01_NAVIGATION_SPECIFICATION.md` §15), search scope, search behavior, autocomplete strategy (WAI-ARIA combobox pattern), typo tolerance and synonym strategy (both Meilisearch-native, synonym content merchandising-owned per the same governance model established for navigation), product ranking, editorial boosting and merchandising rules (capped, time-bound, no fake urgency — mirroring `01_NAVIGATION_SPECIFICATION.md`'s Merchandising Strategy), filtering and sorting strategy (mobile bottom-sheet-with-apply-button vs. desktop live-update, per current mobile filter research), wine discovery and food discovery (including allergen-filter safety-criticality), cross-selling via the still-unmodeled "pairs with" relationship, zero-results recovery (target: under 5% zero-result rate, industry benchmark), empty/loading/error states, accessibility (ARIA live region announcing result count), mobile search, analytics events, SEO (search-results pages `noindex, follow` by default), backend requirements, performance expectations (reusing existing motion tokens and the platform-wide LCP target rather than inventing new numbers), future expansion (recommendations, semantic/natural-language search, AI-assisted discovery, personalization — documented as capability only, none of it built or committed), risks/assumptions, and acceptance criteria. Grounded in limited external research (Baymard, Meilisearch's own documentation, mobile filter UX research, WAI-ARIA live-region guidance, semantic-search industry research) cited inline and in a Sources section — no interfaces, layouts, or wording copied from any source. Flags the "pairs with" relationship as not yet even scoped in `MEDUSA_EXTENSIONS.md`, sharpening the recurring gap already flagged by both prior specifications.
- **`03_SEARCH_SPECIFICATION.md` approved by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** Paul requested five additions before freezing v1.0: **Search Intent** (a table mapping eight named customer intents — exact lookup, category exploration, gifting, occasion shopping, food pairing, budget shopping, premium/luxury browsing, educational discovery — onto mechanisms already specified elsewhere in the document, explicitly without introducing AI or personalization into v1); **Query Understanding** (how search interprets abbreviations, spelling mistakes, plural/singular forms, local Nigerian terminology, and wine/spirit/food terminology, entirely through typo tolerance, synonyms, and full-text matching — no NLU model); **Ranking Philosophy** (a dedicated section stating the priority order — relevance, then availability, then business merchandising, with popularity and freshness explicitly not silent ranking factors in v1 — and the explicit rule that no promotional, business, or availability signal may ever outrank genuine relevance or insert an irrelevant product); **Operational Considerations** (predictable behavior as products become unavailable, prices change, promotions expire, inventory changes, or products are deliberately hidden — and a clear three-way distinction between "unavailable," "hidden," and "deleted"); and a closing **Search Quality Checklist**. Version bumped to 1.0 and status changed directly to **Approved — Frozen**, per Paul's explicit instruction. See `DECISION_LOG.md` for the full entry.
- **`02_HOMEPAGE_SPECIFICATION.md` reviewed for consistency against `01_NAVIGATION_SPECIFICATION.md` and `03_SEARCH_SPECIFICATION.md` (both now frozen), approved and frozen at v1.0 (2026-07-18):** the review confirmed the homepage specification's search-entry-point references match `01_NAVIGATION_SPECIFICATION.md` §9/§15 exactly, its search-results reference matches `03_SEARCH_SPECIFICATION.md` §4 (which cross-references back to it), and every open item it names (age-gate mechanics, curated collection content, the "pairs with" data gap, Meilisearch's pending sign-off) was already properly flagged as a risk/assumption rather than silently assumed — the same standard `01` and `03` themselves met before freezing. No content changes were required; only the header and closing status note were updated. See `DECISION_LOG.md` for the full entry.
- **`04_PRODUCT_LISTING_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: listing philosophy, business/customer objectives, entry points (deferring to `01_NAVIGATION_SPECIFICATION.md`), listing types (category, collection, Food Central menu), a detailed Product Card Behaviour section (available information, interaction behaviour, availability handling, pricing behaviour, promotional indicators, trust indicators, catalog-specific quick-action rules, accessibility), filtering and sorting (reusing `03_SEARCH_SPECIFICATION.md`'s facet system, with a distinct "Featured" default sort rather than "Relevance"), active filter behaviour, a Pagination vs. Infinite Scroll decision (pure infinite scroll rejected; "Load More" with lazy-loading and URL-reflected state adopted, per cited research), mobile/desktop layout decisions (grid columns, left-hand filter sidebar on desktop), merchandising rules and capped promotional content, cross-selling (a single page-level moment, not per-card), Food Central and Wine & Spirits listing-specific behavior, empty/loading/error states, accessibility (card list semantics, the "no link nested inside a link" rule for quick-add actions), analytics, SEO (listing pages as the platform's primary indexable surface, contrasted with search's `noindex`), backend requirements, performance expectations, future expansion (recommendations, AI ranking, personalization, editorial collections, dynamic merchandising — documented as capability only), and acceptance criteria. Grounded in limited external research (Baymard, Smashing Magazine, NN/g, card-design and accessible-card research) cited inline and in a Sources section — no layouts, interfaces, or wording copied. Flags the "pairs with" relationship as a fourth surface depending on the same unscoped `MEDUSA_EXTENSIONS.md` gap.
- **`04_PRODUCT_LISTING_SPECIFICATION.md` approved in principle by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** Paul requested five additions before freezing v1.0: **Listing Intent** (a table mapping seven named browsing intentions — inspiration, buying a known product, comparing, budget shopping, premium/luxury exploration, gift shopping, food pairing — onto mechanisms already specified elsewhere in the document, explicitly without introducing AI or personalization into v1); **Product Card Information Hierarchy** (a dedicated section stating exactly what's always visible, conditionally visible — at most one slot — and never shown on a listing card, consistent across both catalogs); **Merchandising Governance** (an explicit statement of what merchandising can and cannot influence — critically, that a listing's Featured order can never override or be confused with the relevance/ranking authority `01_NAVIGATION_SPECIFICATION.md` and `03_SEARCH_SPECIFICATION.md` already establish — plus promotional limits, expiry behavior, and trust requirements); **Operational Behaviour** (predictable handling of products becoming unavailable, low stock, price changes, promotion expiry, hidden products, and discontinued products, with an explicit three-way "unavailable/hidden/discontinued" distinction); and a closing **Listing Quality Checklist**. Version bumped to 1.0 and status changed directly to **Approved — Frozen**, per Paul's explicit instruction. See `DECISION_LOG.md` for the full entry.
- **`05_PRODUCT_DETAILS_SPECIFICATION.md` drafted in full and frozen directly to v1.0 (2026-07-18)**, per Paul's explicit instruction to complete the full 30-section draft (including the deep-dive treatments requested for Product Information Hierarchy, Wine & Spirits, Food Central, and Trust, plus a closing Product Details Quality Checklist) and then freeze in one pass, rather than the draft-then-separate-refinement-pass sequence used for the four prior specifications. Covers: product detail philosophy, business/customer objectives, entry points (deferring to prior frozen specifications), information architecture, gallery behaviour (zoom required, five-to-eight images across distinct shot types, incl. the wine label-detail shot), a four-tier **Product Information Hierarchy** (always visible / progressively disclosed / optional / never shown, consistent across both catalogs), pricing and availability behaviour (reusing the listing specification's unavailable/hidden/discontinued distinction), a **Wine Product Experience** section (plain-language guidance above structured tasting notes/region/producer/vintage/ABV/pairing/storage, each field shown only where applicable), a **Food Product Experience** section (prep expectations, full ingredient transparency, prominent never-color-alone allergen information, spice level, portion, availability windows, explicit same-day cutoff messaging), product attributes vs. product facts (the data model vs. its structured presentation, kept as two distinct concepts), pairing recommendations (cross-catalog, editorial, confirmed as the fifth specification depending on the still-unscoped "pairs with" data), related products (same-catalog, buildable now via existing category/collection membership), cross-selling (a v1-appropriate Gift Wrap add-on, with algorithmic cross-selling deferred), quantity selection (a stepper, stock-capped for Wine & Spirits, uncapped-by-stock for Food Central), add-to-cart behaviour (persistent confirmation, sticky mobile control), a **Trust Signals** section (authenticity, freshness, availability accuracy, pricing transparency, delivery expectations, returns/refunds — correctly deferring the alcohol return policy to the still-open business decision — and lightweight age-confirmation reminders, all explicitly subordinate to trust over conversion pressure), delivery and pickup information (pickup confirmed for Food Central only; not assumed for Wine & Spirits, since no prior document establishes it), a Reviews Strategy explicitly confirming no review system exists in v1, empty/error states, accessibility, analytics, SEO (Product/Offer structured data, contrasted with search's `noindex`), backend requirements, a **Future Expansion** section (customer reviews, expert reviews, recommendations, personalization, AI assistance, richer educational content — documented as capability only), acceptance criteria, and the closing **Product Details Quality Checklist**. Grounded in limited external research (Baymard, wine-ecommerce UX research, digital-menu allergen-transparency research, progressive-disclosure and cart-feedback research) cited inline and in a Sources section — no layouts, interfaces, or wording copied. See `DECISION_LOG.md` for the full entry.
- **`06_CART_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: cart philosophy (prepare, never surprise — nothing changes silently), business/customer objectives, entry points (deferring to `01` and `05`), a cart information architecture grouping line items by fulfillment leg, a deep-dive **Mixed Cart Behaviour** section implementing `PRODUCT_BLUEPRINT.md` §18's named risk directly (two fulfillment-leg groups, never a merged delivery promise, scheduling deferred to checkout, five specific open business decisions explicitly flagged rather than invented — delivery mechanism, Lagos-area definition, slot parameters, COD, hard age-recheck), quantity management (stock-capped stepper for Wine & Spirits, uncapped for Food Central, reusing `05`'s exact control pattern), price calculations (explicit about what's known now vs. calculated at checkout), promotions (no cart-level coupon system assumed, no fabricated urgency), delivery and pickup eligibility (informational in the cart, authoritative at checkout), availability changes and out-of-stock behaviour (kept as two distinct, non-overlapping sections), a Saved-for-Later Strategy (a new, not-yet-scoped recommendation, explicitly flagged as such), gift wrapping (reusing `05`'s decision, cart-editable, Wine & Spirits only), cart persistence (guest session + logged-in cross-device, native Medusa Cart/Customer association), estimated delivery messaging (general, never merged across fulfillment legs), cross-selling (single restrained pairing suggestion, single-catalog carts only), a **Trust Signals** section naming every mechanism explicitly (price changes, inventory changes, unavailable products, scheduled delivery, pickup, delivery limitations, age-restricted products), empty/loading/error states, accessibility (ARIA live-region cart-total announcements, closing a documented screen-reader gap), analytics, SEO (`noindex`, same treatment as search results), backend requirements (flagging Saved-for-Later as newly recommended and not yet scoped in `MEDUSA_EXTENSIONS.md`), a **Future Expansion** section (loyalty, subscriptions, saved carts, shared carts, gift registries, corporate ordering — capability only), risks/assumptions, acceptance criteria, and a numbered **Cart Quality Checklist** (§29, unlike prior specifications' unnumbered end-of-document checklists). Grounded in limited external research (Baymard, cart-abandonment statistics, Shopify/Optimizely split-shipment documentation, persistent-cart research, ARIA live-region and screen-reader accessibility research) cited inline and in a Sources section — no layouts, interfaces, or wording copied. Confirms the "pairs with" gap as a dependency of a sixth specification.
- **`06_CART_SPECIFICATION.md` approved in principle by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** Paul requested six areas of expansion before freezing v1.0, without redesigning or removing any existing section: a new **Customer Decision States** section classifying every cart message into five types (informational, recommendation, warning, blocking condition, recoverable error), each with why/when/impact/expected action; an expanded **Mixed Cart Behaviour** (§6) naming four concrete mechanisms that prevent fulfillment-group confusion, plus a Future Operational Expansion note confirming the two-group model generalizes to a hypothetical future fulfillment leg without redesign; a new **Pricing Transparency** section consolidating every cart amount (confirmed vs. estimated/unknown) into one table; a new **Cart Recovery** section covering nine named scenarios (expired sessions, removed/discontinued/hidden products, inventory and quantity changes, price updates, failed updates, network interruptions), governed by an explicit intent-preservation principle; an expanded §23 Accessibility (keyboard navigation, focus management, screen-reader coverage beyond totals, per-control touch targets, error announcements, and dynamic-update announcements); and an expanded §29 Cart Quality Checklist (pricing clarity, fulfillment clarity, operational transparency, recovery behaviour, and checkout readiness added as explicit checks). Version bumped to 1.0 and status changed to **Approved — Frozen**, per Paul's explicit instruction. See `DECISION_LOG.md` for the full entry.
- **Repository reconciliation, round two (2026-07-18):** this session's designated branch (`claude/medusa-repo-clone-ut5dl5`) had fallen behind a second, independently-progressed session branch (`claude/project-onboarding-status-p35u3v`), which already carried the first repository reconciliation, the documentation governance hardening pass, and specifications 01–06 through drafting, refinement, and freeze. Discovered via `git branch -r`/`git log --all` rather than assumed, per `AI_HANDOFF.md`'s own prescribed discipline. No PR existed for either branch. Reconciled via a clean fast-forward merge — no history rewritten, nothing squashed, every commit from both branches preserved — then pushed. Validated post-merge: all four frozen Phase 0 documents and all six frozen specifications unchanged; no broken links; no conflict markers; every document status header consistent with `README.md`'s specification table and this document. See `DECISION_LOG.md` for the full entry.
- **`07_CHECKOUT_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: checkout philosophy (confirms what the cart already prepared, never introduces a new surprise), business/customer objectives, entry points (deferring to `06`), a checkout information architecture naming the logical step sequence (Order Summary → Contact & Delivery Address → Delivery Method & Slot Selection → Payment → Review & Confirm → Order Confirmation), guest checkout behaviour (post-purchase account creation only, never before or during), address capture behaviour (freeform/landmark-friendly fields per the Nigerian-context findings, minimum-necessary field set), a **Mixed-Order Checkout Behaviour** section directly answering the risk `06_CART_SPECIFICATION.md` §28 named (one address per order in v1, the cart's two fulfillment-leg groups persisting through every checkout step, a new named **blocking condition** when a non-Lagos address meets a Food Central item), delivery method selection, delivery slot selection behaviour (calendar-grid per `DELIVERY_MODEL.md`, same-day cutoff, capacity enforced at both selection and final submission), address-based delivery eligibility enforcement (the authoritative Lagos-only check the cart could only state informally), availability re-validation at checkout, pricing and fee calculation (completing the cart's Pricing Transparency table — delivery fee and tax become confirmed here), payment behaviour (provider-agnostic, cash-on-delivery explicitly flagged open, not invented), an age-verification backstop (restates the reminder already established in `05`/`06`; the hard-recheck-at-confirmation question remains explicitly open, with only *where* it would attach specified), an order review step, order confirmation (post-purchase account creation, confirmation notification — channel undecided), trust signals, empty/invalid checkout states, loading/error states (including the highest-stakes case: a backend failure after payment has been authorized), accessibility, responsive behaviour, analytics events, SEO (`noindex`, same treatment as cart/search), backend requirements, performance expectations, future expansion (capability only — additional payment methods, saved payment methods, split/partial payments, the hard age-recheck if adopted, real-time fee estimation, multi-address checkout), risks/assumptions, and acceptance criteria. Grounded in limited external research (Baymard on checkout flow optimization, guest-checkout prominence, form-field minimization, mobile checkout, and order-confirmation-page best practices) cited inline and in a Sources section — no layouts, interfaces, or wording copied. Confirms five business decisions already flagged by `06_CART_SPECIFICATION.md` remain open and unresolved at the checkout level (Wine & Spirits delivery mechanism, Lagos delivery-area definition, delivery-slot parameters, cash-on-delivery, hard age-recheck), plus the payment-provider and notification-channel gaps as launch-critical backend dependencies.
- **`07_CHECKOUT_SPECIFICATION.md` approved by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** reviewed against all four frozen Phase 0 documents and all six frozen prior specifications with no inconsistency found; existing sections were not rewritten. Paul requested four additions before freezing v1.0, without redesigning or removing any existing section: a new **Checkout Intent & Customer Decision States** treatment — a Checkout Intent table (Fast Completion, Careful Review, Mixed-Order Resolution, Recovery) mapping recognizable customer intents onto mechanisms already specified, explicitly without AI or personalization, plus a Customer Decision States table that **reuses `06_CART_SPECIFICATION.md`'s exact five-state taxonomy** (informational, recommendation, warning, blocking condition, recoverable error) instantiated with checkout-specific triggers, rather than inventing a parallel vocabulary; a new **Payment State Behaviour** section specifying pending, failed, cancelled, expired, and retry as distinct payment states — a genuine gap the first draft left unaddressed, since local payment methods (bank transfer, USSD) commonly involve an asynchronous confirmation step a simple card charge does not; a new **Checkout Recovery** section (five named scenarios — browser closed mid-checkout, session/cart expiry, payment-redirect return, network interruption during submission, blocking-condition resolution) governed by the same intent-preservation principle already established in `06_CART_SPECIFICATION.md`'s Cart Recovery section; and a closing **Checkout Quality Checklist** mirroring the pattern already established by every prior frozen specification. Small in-place expansions accompanied these additions: §16 (Order Review Step) gained an explicit "review builds confidence, introduces no new decision" framing; §18 (Trust Signals) gained a cross-reference to pending-payment reassurance; §21 (Error States) gained a cross-reference to the new recovery sections; §22 (Accessibility) gained two bullets (focus return after external payment redirects, live-region announcements for payment-state changes); §29 (Risks) and §30 (Acceptance Criteria) were each updated to reflect the new sections without altering any previously-flagged open business decision. Version bumped to 1.0 and status changed directly to **Approved — Frozen**, per Paul's explicit instruction. See `DECISION_LOG.md` for the full entry.
- **`08_CUSTOMER_ACCOUNT_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: account philosophy (an account is a convenience a customer opts into, never a requirement), business/customer objectives, entry points (deferring to `01_NAVIGATION_SPECIFICATION.md` §16), account information architecture, account creation behaviour (minimum-necessary fields; post-purchase creation pre-filled from the just-completed order; no social/third-party login, per `TECH_STACK.md`), guest-to-account conversion (never required, never blocking, no consequence for declining), login & logout behaviour (no account-existence leakage on failure), password reset behaviour, email verification behaviour (never a barrier to using the account), profile management, saved addresses (reusing `07_CHECKOUT_SPECIFICATION.md` §7's freeform/landmark field structure), order history (one unified list spanning both catalogs, never split by product line), order details (a permanent, unaltered record; no returns pathway specified pending the still-open alcohol return-policy decision), reordering behaviour (re-validates price/availability, never a blind copy — directly implementing `USER_FLOWS.md` Flow 7), a Saved Items note (confirming `06_CART_SPECIFICATION.md` §14's Saved-for-Later remains cart-level, not duplicated into a separate account-level wishlist), notification preferences (gated on the still-open notification-channel decision), Privacy & Security (data visibility/correction; retention/NDPR specifics flagged as an open legal question, not invented), Account Deletion & Deactivation (a request pathway exists; the exact policy is flagged as open, mirroring how the alcohol return policy is treated in `05_PRODUCT_DETAILS_SPECIFICATION.md`), Trust Considerations, Customer Decision States (reusing the exact five-state taxonomy already established in `06`/`07`), empty/loading/error states, accessibility, analytics, SEO (`noindex`, same treatment as cart/checkout), backend requirements, performance expectations, a **Future Expansion & Explicitly Out of Scope** section explicitly naming loyalty, wishlists, reviews, subscriptions, personalization, social login, and AI features as not introduced (each traced to why, per direct instruction), risks/assumptions, a closing **Account Quality Checklist**, and acceptance criteria. Grounded in limited external research (Nielsen Norman Group on registration/login and password reset, Baymard on accounts/self-service, order history, order returns, and address-book UX) cited inline and in a Sources section — no layouts, interfaces, or wording copied. Confirms three genuine open business/legal decisions (data-retention/NDPR specifics, account deletion-vs-deactivation policy, notification-channel choice), none resolved here.
- **`08_CUSTOMER_ACCOUNT_SPECIFICATION.md` approved by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** reviewed against all four frozen Phase 0 documents and all seven frozen prior specifications with no inconsistency found; existing sections were not rewritten. Paul requested additions in three areas before freezing v1.0: a new **Account Lifecycle** section consolidating the account's state transitions (Guest → Registered → Active → Deactivated → Deleted) into one view, including reactivation mechanics for a deactivated account and an explicit rule that an in-progress order is never affected by a lifecycle transition (deactivating or deleting an account is never a cancellation of a commitment already made to a courier, kitchen, or payment provider); a new **Session & Security Behaviour** section (password-change session invalidation, permitted concurrent sessions across devices, login rate-limiting/lockout, and step-up reauthentication for sensitive actions — email change, deletion request); and a new **Account Recovery** section (five named scenarios — forgotten password, lost access to the registration email, expired reset/verification links, session expiry mid-edit, lockout — governed by the same intent-preservation principle already established in `06_CART_SPECIFICATION.md`'s Cart Recovery and `07_CHECKOUT_SPECIFICATION.md`'s Checkout Recovery sections). Small in-place expansions accompanied these additions: §12 (Saved Addresses) gained a bullet on default-address-deletion behavior; §15 (Reordering Behaviour) gained an opening "reorder philosophy" framing; §16 (Notification Preferences) gained a bullet on the pre-preference default state; §19 (Trust Considerations) gained a cross-reference to session security as a trust mechanism; §22 (Accessibility) gained two bullets (focus after an irreversible action, live-region announcements for account state changes); §28 (Risks) and §29 (Account Quality Checklist) and §30 (Acceptance Criteria) were each updated to reflect the new sections without altering any previously-flagged open business/legal decision. A standalone "Operational Behaviour" section was deliberately not added — its substance (the in-progress-order/lifecycle interaction) was folded into Account Lifecycle instead, to avoid colliding with the different, already-established meaning "Operational Behaviour" carries in `04_PRODUCT_LISTING_SPECIFICATION.md` and `06_CART_SPECIFICATION.md` (product availability, not account state). Version bumped to 1.0 and status changed directly to **Approved — Frozen**, per Paul's explicit instruction. See `DECISION_LOG.md` for the full entry.
- **`09_FOOD_ORDERING_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: food ordering philosophy (speed never achieved by hiding a real constraint; Food Central as a premium subsidiary experience, not a bolted-on delivery app), business/customer objectives, entry points (deferring to `01_NAVIGATION_SPECIFICATION.md` §14, `02_HOMEPAGE_SPECIFICATION.md` §8.5), food catalog structure and menu organization (menu-like, not taxonomy-like, per `01_NAVIGATION_SPECIFICATION.md` §14), a three-state availability model (available now / available to schedule / unavailable — distinct from Wine & Spirits' stock-based unavailable/hidden/discontinued taxonomy), cook-to-order workflow (a minimum customer-facing status progression: Order Received → Preparing → Ready/Out for Delivery → Completed, exact granularity flagged open), preparation status and estimated preparation times, same-day ordering rules and cutoff behaviour (restating `DELIVERY_MODEL.md`'s explicit cutoff findings), scheduling orders (reusing `07_CHECKOUT_SPECIFICATION.md` §10's calendar-grid mechanism, not redefining it), pickup workflow (equal weight to delivery, per `DELIVERY_MODEL.md`), delivery workflow customer-facing only (operational logistics explicitly deferred to `10_DELIVERY_SPECIFICATION.md`, not yet drafted), mixed wine & food orders (reusing `06_CART_SPECIFICATION.md` §6's and `07_CHECKOUT_SPECIFICATION.md` §8's exact two-fulfillment-group model, not reinventing it), ingredient transparency and allergen information (reusing `05_PRODUCT_DETAILS_SPECIFICATION.md` §11's exact requirements), quantity handling and customisation (explicitly out of scope, per direct instruction — no modifier/substitution system introduced), menu availability changes and ingredient shortages (an "86'd" dish as a normal operational reality, not an error state), customer expectations during preparation, food-specific trust considerations, customer decision states (reusing the exact five-state taxonomy already established in `06`/`07`/`08`), empty/loading/error states, accessibility (live-region announcements for the cutoff countdown and status progression, reusing `06`/`07`'s mechanism), a **Kitchen Operational Considerations** section (operating hours, capacity-driven early closure — deliberately distinguished from the different "Operational Behaviour" meaning already established in `04`/`06`), analytics, SEO (`noindex` for order-status views, indexable for menu/dish pages), backend requirements (flagging the food-attributes and delivery-slot modules as not yet built), performance expectations, a **Future Expansion & Explicitly Out of Scope** section explicitly naming table booking, dine-in, loyalty, subscriptions, AI recommendations, reviews, recipe content, and customization as not introduced, risks/assumptions, a closing **Food Ordering Quality Checklist**, and acceptance criteria. Grounded in limited external research (Baymard on food delivery/takeout UX benchmarking) cited inline and in a Sources section — no layouts, interfaces, or wording copied. Confirms several genuine open operational decisions (order-status vocabulary, cutoff/slot parameters, scheduling horizon, kitchen operating hours, allergen-data ownership), none resolved here.
- **`09_FOOD_ORDERING_SPECIFICATION.md` approved by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** reviewed against all four frozen Phase 0 documents and all eight frozen prior specifications with no inconsistency found; existing sections were not rewritten. Paul requested additions in four areas before freezing v1.0: a new **Food Ordering Intent** section (a table mapping five recognizable customer intents — craving-driven speed, menu discovery, planning ahead, mixed-order pairing, recovery — onto mechanisms already specified elsewhere in the document, mirroring `07_CHECKOUT_SPECIFICATION.md`'s Checkout Intent pattern, explicitly without introducing AI or personalization); a new **Freshness & Quality Philosophy** section naming the connective principle between `BRAND_IDENTITY.md`'s and `EXPERIENCE_PRINCIPLES.md`'s existing freshness/quality language and this document's ordering-specific behaviour (no dish presented as held finished in stock, prep-time honesty as a quality signal, quality communicated through accurate information rather than a numeric rating or score); a new **Availability Transition Behaviour** section unifying the transition rules between the three availability states first specified in §6, §9, §16, and §22 (a transition is always driven by a real kitchen event, reflected the moment it's known, never silently drops a cart item, and does not require proactively notifying a customer when a dish returns to available); and a new **Food Ordering Recovery** section (five named scenarios — mid-browse interruption, an item becoming unavailable before checkout, a same-day cutoff passing on an unpurchased cart item, a scheduled order's capacity changing before its date arrives, network interruption during placement — governed by the same intent-preservation principle already established in `06_CART_SPECIFICATION.md`'s Cart Recovery, `07_CHECKOUT_SPECIFICATION.md`'s Checkout Recovery, and `08_CUSTOMER_ACCOUNT_SPECIFICATION.md`'s Account Recovery sections). Small in-place expansions accompanied these additions: §13 (Mixed Wine & Food Orders) gained a bullet confirming each fulfillment group's re-validation is independent, so a Food Central item becoming unavailable never invalidates the Wine & Spirits portion of the same order or vice versa; §21 (Accessibility) gained a bullet extending the existing live-region pattern to any availability-state transition, not only the cutoff countdown and order-status progression; §29 (Food Ordering Quality Checklist) and §30 (Acceptance Criteria) were each updated to reflect the new sections without altering any previously-flagged open operational decision. Version bumped to 1.0 and status changed directly to **Approved — Frozen**, per Paul's explicit instruction. See `DECISION_LOG.md` for the full entry.
- **`10_DELIVERY_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — all 30 required sections: delivery philosophy (delivery is where every earlier promise is kept or broken; nothing about delivery is silently escalated to a third party), business/customer objectives, entry points and scope boundary (explicitly not redefining checkout's, the cart's, or Food Ordering's already-frozen pre-order mechanics), delivery coverage restating the Lagos-vs-nationwide split, Wine & Spirits' nationwide workflow (standard warehouse dispatch, delivery mechanism still open), Food Central's Lagos workflow (company-owned riders only, no third-party courier or dispatch platform), pickup workflow (operational ready-time honesty, equal care to delivery), same-day and scheduled delivery fulfillment (restating checkout's and Food Ordering's customer-facing mechanics, specifying only the post-booking operational commitment), a **Delivery Status Progression** section specifying Wine & Spirits' own status vocabulary (Order Placed → Dispatched → In Transit → Delivered) for the first time, since `09_FOOD_ORDERING_SPECIFICATION.md` had deferred nationwide dispatch status to this document, delivery tracking expectations (status-based, explicitly not live GPS), address validation and landmark handling (operational consequence of checkout's freeform address capture — direct customer contact to resolve ambiguity, no automated geocoding assumed), failed delivery and customer-unavailable scenarios (Food Central's perishable-order urgency named as distinct from Wine & Spirits'), delivery rescheduling and cancellation (the pre-/post-fulfillment-start cancellation distinction named, exact cutoff policy flagged open), mixed wine-and-food delivery handling (reusing the exact two-fulfillment-leg independence model), delivery fees (confirmed at checkout, restated not recalculated here), proof of delivery and age verification at delivery (a new, explicitly conservative open question — whether age is physically re-verified at the point of hand-off, distinct from but related to `07_CHECKOUT_SPECIFICATION.md` §15's order-confirmation recheck question), customer communication (proactive WhatsApp/SMS-style messaging, channel still undecided), customer decision states (reusing the exact five-state taxonomy), empty/loading/error states, accessibility (live-region status announcements, reusing the platform-wide mechanism), a **Delivery Operations Considerations** section (rider capacity, geographic edge cases — deliberately distinguished from `04`/`06`'s "Operational Behaviour" and `09`'s "Kitchen Operational Considerations"), analytics, SEO (`noindex`, consistent with every other customer-specific surface), backend requirements (flagging the delivery-slot module, notification provider, and proof-of-delivery mechanism as not yet built), performance expectations, a **Future Expansion & Explicitly Out of Scope** section explicitly naming third-party courier integration, live GPS tracking, route optimisation, delivery marketplace features, AI dispatch, autonomous delivery, and locker pickup as not introduced, risks/assumptions, a closing **Delivery Quality Checklist**, and acceptance criteria. Grounded in limited external research (Baymard on order confirmation and food-delivery/takeout UX, already cited by prior specifications) cited inline and in a Sources section — no layouts, interfaces, or wording copied. Confirms several genuine open operational and business decisions (Wine & Spirits' delivery mechanism, the exact Lagos delivery-area definition, the delivery-fee schedule, the failed-delivery-attempt policy, the cancellation-cutoff policy, and doorstep age verification), none resolved here.
- **`10_DELIVERY_SPECIFICATION.md` approved by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** reviewed against all four frozen Phase 0 documents and all nine frozen prior specifications with no inconsistency found; existing sections were not rewritten. Paul requested additions in four areas before freezing v1.0: a new **Delivery Intent** section (a table mapping five recognizable customer intents — reliable arrival, self-collection, mixed-order clarity, plan adjustment, recovery — onto mechanisms already specified elsewhere in the document, mirroring `07_CHECKOUT_SPECIFICATION.md`'s Checkout Intent and `09_FOOD_ORDERING_SPECIFICATION.md`'s Food Ordering Intent pattern); a new **Delivery Trust & Professionalism** section naming the connective principle between `PRODUCT_BLUEPRINT.md` §11's "sold and delivered by us directly" claim and the physical delivery moment itself (a rider or pickup interaction as the single most concrete proof of that claim; landmark-first addressing named explicitly as a deliberate trust-building design choice, not a tolerated limitation; no rider rating/review mechanism introduced, consistent with `05_PRODUCT_DETAILS_SPECIFICATION.md`'s Reviews Strategy); a new **Customer Expectations During Delivery** section mirroring `09_FOOD_ORDERING_SPECIFICATION.md`'s "Customer Expectations During Preparation," applied to the delivery/pickup waiting period; and a new **Delivery Recovery** section (five named scenarios — a failed delivery attempt, an unresolved address ambiguity, a mixed order's one leg failing while the other succeeds, a status update failing to load, a reschedule or cancellation — governed by the same intent-preservation principle already established in `06`'s Cart Recovery, `07`'s Checkout Recovery, `08`'s Account Recovery, and `09`'s Food Ordering Recovery sections). Small in-place expansions accompanied these additions: §8 (Pickup Workflow) gained an opening "pickup is a deliberate parity option, not a fallback" framing; §10 (Delivery Status Progression) gained two bullets (a transition moves forward only, never silently reverted; every transition is timestamped internally); §18 (Customer Communication) gained an opening governing-principle framing ("a delivery message exists to remove uncertainty, never to create it"); §21 (Accessibility) gained a bullet on focus management after a reschedule, cancellation, or recovery action; §29 (Delivery Quality Checklist) and §30 (Acceptance Criteria) were each updated to reflect the new sections without altering any previously-flagged open operational decision. Version bumped to 1.0 and status changed directly to **Approved — Frozen**, per Paul's explicit instruction. See `DECISION_LOG.md` for the full entry.
- **`11_ADMIN_WORKFLOWS_SPECIFICATION.md` drafted in full (v0.1, 2026-07-18)** — the eleventh and final planned Phase 1 specification, covering all 30 required sections: admin workflows philosophy (the admin surface exists to make every customer-facing promise operationally true), business/operational objectives, entry points and scope boundary (the Medusa Admin dashboard itself, extended via widgets/routes, never a separate application), a Dashboard section, Product Management (wine/food attribute data entry, allergen-verification responsibility flagged open), Category & Collection Management (the staff-facing workflow for the content control `01_NAVIGATION_SPECIFICATION.md`'s Governance table already assigns to merchandising), Inventory Management (stock counts for Wine & Spirits; availability-state/86'ing workflow for Food Central), Pricing Management, Promotions Management (cap/expiry enforcement, no fake urgency), Order Management (the unified, two-fulfillment-leg mixed-order view), a **Food Order Workflow** section (the kitchen-facing counterpart to `09_FOOD_ORDERING_SPECIFICATION.md` §7's cook-to-order progression), a **Delivery Management** section (the staff-facing counterpart to `10_DELIVERY_SPECIFICATION.md`, rider assignment remaining manual/operational), Customer Management (staff-facing support view, respecting `08_CUSTOMER_ACCOUNT_SPECIFICATION.md`'s data-ownership principle), a **Staff Roles & Permissions** section explicitly flagging whether granular, role-differentiated permissions are needed as a genuine open decision (Medusa's native model distinguishes only customer and admin-user), Content Management, a **Search Merchandising Workflow** section (the staff-facing counterpart to `03_SEARCH_SPECIFICATION.md`'s Merchandising Rules and Ranking Philosophy), Reports & Analytics, staff-facing Notifications (distinct from customer notifications), Audit Logging (attributability as a baseline accountability requirement), Security, Operational Decision States (reusing the five-state taxonomy), empty/loading/error states, Accessibility (custom admin extensions follow `DESIGN_SYSTEM.md`; Medusa's native admin UI is explicitly out of that scope), backend requirements, performance expectations, a **Future Expansion & Explicitly Out of Scope** section explicitly naming granular RBAC, a dispatch/route-optimization module, a BI platform, a vendor-facing capability, and AI-assisted decision-making as not introduced, risks/assumptions, a closing **Admin Workflows Quality Checklist**, and acceptance criteria. Contains no UI mockups, wireframes, implementation code, database schema, or API design, per direct instruction. Confirms several genuine open business/operational decisions (allergen/ingredient data-verification responsibility, staff-permission granularity, audit-log retention and granularity, exact report definitions, staff-facing alert thresholds), none resolved here.
- **`11_ADMIN_WORKFLOWS_SPECIFICATION.md` approved by Paul, then finalized to v1.0 with a refinement pass (2026-07-18):** reviewed against all four frozen Phase 0 documents and all ten frozen prior specifications with no inconsistency found; existing sections were not rewritten. Paul directed additions where they genuinely strengthened the specification, adding four new sections: an **Admin Workflow Intent** section (a table mapping five recognizable staff intents — keeping the catalog accurate, running a promotion within the rules, fulfilling and resolving an order, supporting a customer, recovering from an interruption — onto mechanisms already specified elsewhere in the document, mirroring `07`'s Checkout Intent, `09`'s Food Ordering Intent, and `10`'s Delivery Intent pattern); an **Operational Trust & Accountability** section naming staff accountability as the internal twin of `PRODUCT_BLUEPRINT.md` §11's "sold and delivered by us directly" claim, and explicitly confirming that audit logging exists for accountability and troubleshooting, never as a staff-performance-scoring mechanism; a **Cross-Catalog Operational Parity** section extending `BUSINESS_RULES.md`'s and `01_NAVIGATION_SPECIFICATION.md`'s customer-facing "equal prominence" principle to internal tooling investment for the first time (equal seriousness, not identical workflows, since Wine & Spirits' and Food Central's operational realities genuinely differ); and an **Admin Workflow Recovery** section (four named scenarios — a session expiring mid-edit, a multi-step action partially completing then failing, a save failing during peak load, a network interruption during an order/food-order status update — governed by the same intent-preservation principle already established in `06`'s Cart Recovery, `07`'s Checkout Recovery, `08`'s Account Recovery, `09`'s Food Ordering Recovery, and `10`'s Delivery Recovery sections). Small in-place expansions accompanied these additions: §5 (Dashboard) gained a bullet on cross-catalog parity; §20 (Audit Logging) gained a bullet confirming the mechanism is not a staff-scoring device; §24 (Accessibility) gained a bullet on focus management after a save or recovery action; §29 (Admin Workflows Quality Checklist) and §30 (Acceptance Criteria) were each updated to reflect the new sections without altering any previously-flagged open operational decision. No previously-flagged open business/operational decision was resolved or silently assumed. Version bumped to 1.0 and status changed directly to **Approved — Frozen**, per Paul's explicit instruction. **With this freeze, all 11 planned Phase 1 specifications are complete.** See `DECISION_LOG.md` for the full entry.
- **`IMPLEMENTATION_PLANNING.md` created as the master governing document for Phase 2 (2026-07-18)** — not an implementation document itself: it contains no API design, database schema, component design, or code. Defines: the purpose and scope of Phase 2 (translating the 11 frozen specifications into concrete implementation plans before development begins); an Engineering Philosophy (specification-driven, Medusa-native first, one module per plan, open business decisions inherited not re-litigated, plan for testability, smallest sufficient plan); this tier's place in `DOCUMENTATION_GOVERNANCE.md` §3's existing hierarchy; a **six-tier hierarchy of implementation documents** — Tier A (Foundational Reconciliation of the five pre-existing implementation-tier documents against the now-frozen specifications), Tier B (Module Data Planning, one document per custom module), Tier C (API Contract Planning, grouped by product surface), Tier D (Integration Planning, one per third-party integration), Tier E (Testing & Acceptance Planning, traceable to each specification's Acceptance Criteria), and Tier F (Rollout Sequencing, an eventual `ROADMAP.md` update, not a new document); Dependencies (every Tier B–D document traces to specific frozen-specification sections and inherits, never resolves, open business decisions); a Creation Order (Tier A first, since every later tier cites it; then B, C, D by launch-criticality; then E; then F); an Approval Workflow reusing Phase 1's exact two-stage pattern (draft → refinement pass → freeze, or draft → direct freeze); Versioning and Document Lifecycle sections confirming Phase 2 inherits Phase 1's conventions unchanged; a per-surface/per-module **Exit Criteria** section gating when actual development may begin; and a closing Quality Checklist. Explicitly does not begin any Tier A–F document itself — per `DOCUMENTATION_GOVERNANCE.md` §5/§8, none begins without Paul's explicit direction.
- **Tier A — Foundational Reconciliation complete (2026-07-18):** `docs/implementation-planning/TIER_A_FOUNDATIONAL_RECONCILIATION.md` (v1.0, Approved) reviewed all 11 frozen Product Specifications against the five pre-existing implementation-tier documents (`ARCHITECTURE.md`, `TECH_STACK.md`, `MEDUSA_EXTENSIONS.md`, `API_DECISIONS.md`, `ROADMAP.md`) plus `PRODUCT_CATALOG.md`, `INFORMATION_ARCHITECTURE.md`, `DELIVERY_MODEL.md`, `BUSINESS_RULES.md`, and `USER_FLOWS.md`. Found every foundational architectural assumption still valid, no outright-obsolete assumption, and **one genuine conflict**: `PRODUCT_CATALOG.md` frames the "pairs with" relationship as a narrow wine-side attribute field, while six frozen specifications treat it as a general, unscoped product-relationship module — flagged for correction when that module's Tier B document is drafted, not resolved here. Identified nineteen new or newly-sharpened missing-work items, consolidated into one **Definitive Implementation Baseline** table (20 rows) that every future Tier B–F document will reference, most significantly: the "pairs with" module has **no `MEDUSA_EXTENSIONS.md` entry at all** despite being the most cross-referenced missing infrastructure in all of Phase 1 (depended on by six specifications); Saved-for-Later remains unscoped; and `11_ADMIN_WORKFLOWS_SPECIFICATION.md` surfaced four entirely new, previously-unscoped backend needs (audit logging, staff-facing internal notifications, reports/analytics aggregation, and staff role/permission granularity) plus `10_DELIVERY_SPECIFICATION.md`'s proof-of-delivery mechanism and `09_FOOD_ORDERING_SPECIFICATION.md`'s kitchen-operating-hours configuration. Also flagged a `ROADMAP.md` sequencing gap: neither `08_CUSTOMER_ACCOUNT_SPECIFICATION.md` nor `11_ADMIN_WORKFLOWS_SPECIFICATION.md` is currently named in any backend build phase — explicitly deferred to Tier F (Rollout Sequencing) to fix, not resolved here. No Frozen document was modified; no API, database, or code was designed or written.
- **`docs/implementation-planning/TIER_B_PRODUCT_RELATIONSHIP_MODULE.md` drafted in full (v1.0, Draft, 2026-07-18)** — the first Tier B document, addressing Tier A's highest-priority finding. Purely architectural, per direct instruction: no database design, no API specification, no code. Defines why the module exists (six frozen specifications depend on it with no prior architecture), business justification (`PRODUCT_BLUEPRINT.md`'s vision, `EXPERIENCE_PRINCIPLES.md` #10 "Food and Wine Should Feel Connected"), responsibilities (a general, bidirectionally-discoverable curated association between two products, resolving live to Product data), explicit non-responsibilities (does not replace Related Products, does not rank/influence search, does not compute algorithmic recommendations, does not set pricing, does not determine availability), relationship types (one required kind for v1 — cross-catalog pairing — with the concept of "kind" left open for a future addition without restructuring), supported catalogs, ownership (module shape is engineering's; which pairings exist is merchandising's, per the same governance split `01_NAVIGATION_SPECIFICATION.md` already established), and integration points with all nine other specifications named in the instruction — including two **explicit non-integrations** confirmed rather than assumed: Checkout (`07`) has zero dependency on this module, and Admin Workflows (`11`) has not yet specified a staff curation workflow for it (a named, unresolved dependency, not invented here). Also covers CMS responsibilities (one-way sync, optional, not required for v1), merchandising responsibilities, future extensibility (additional relationship kinds, a future business line), risks (the Tier A conflict recurring if implementation reverts to `PRODUCT_CATALOG.md`'s narrower framing; no staff workflow yet existing; scope creep toward algorithmic recommendation), dependencies (the Product module, a soft dependency on the attribute modules' field lists, a future admin-workflow document, and Paul's explicit confirmation before further Tier B/C work), a Quality Checklist, and architecture-level Acceptance Criteria (no table, field, or endpoint named anywhere in the document, confirmed by direct review). Status intentionally **Draft, not In Progress/0.x** — per the explicit instruction governing this document, awaiting Paul's review before any refinement pass or approval.
- **`docs/implementation-planning/TIER_B_PRODUCT_RELATIONSHIP_MODULE.md` approved by Paul and finalized to v1.0 — Approved (2026-07-18):** reviewed against `IMPLEMENTATION_PLANNING.md`, `TIER_A_FOUNDATIONAL_RECONCILIATION.md`, `PRODUCT_BLUEPRINT.md`, `BUSINESS_RULES.md`, `MEDUSA_EXTENSIONS.md`, and all 11 frozen specifications, with no existing section rewritten. One genuine cross-reference gap found and corrected: `01_NAVIGATION_SPECIFICATION.md` §13/§14/§19 depends on this module too, for a distinct purpose (cross-linking/wayfinding, not a merchandising suggestion) — a new **Integration with Navigation** section was added (the document's only new section) to close the gap without disturbing the eight consuming-surface sections already reviewed. A second clarification was added to Dependencies: a future `MEDUSA_EXTENSIONS.md` entry formally registering this module remains outstanding, deferred to when Tier C or a data-model follow-on is ready — not created by this finalization. Status changed to **Approved**, per Paul's explicit instruction (using "Approved," not "Frozen," per his literal wording). No implementation dependency requiring resolution was discovered beyond what was already tracked; the two items above are recorded in "Decisions awaiting Paul's approval," below, not resolved.
- **`docs/implementation-planning/MODULE_INVENTORY.md` created (v1.0, Approved, 2026-07-18)**, per Paul's explicit recommendation — a living, single-page index of every module, integration, and backend capability identified anywhere in `/docs`: ten native Medusa modules, five custom modules (including the now-approved Product Relationship Module), two provider modules, two third-party integrations, and thirteen not-yet-scoped items, each tagged by type, status, dependencies, and launch-criticality. Answers, at a glance, which modules exist, which are native/custom/extension, which depend on others, which are optional, and which are launch-critical. Not itself a Tier B document — it indexes them, updated in the same change as any future module-level document.
- **`docs/implementation-planning/TIER_B_WINE_ATTRIBUTES_MODULE.md` drafted in full (v1.0, Draft, 2026-07-18)** — the second Tier B document, selected via a six-criteria priority evaluation recorded in `DECISION_LOG.md`: the wine-attributes module (`MEDUSA_EXTENSIONS.md` #1) is tied with `food-details` for the most frozen-specification dependencies (five each), but wins on architectural centrality and dependency ordering — `ROADMAP.md`'s own backend build sequence places it (Phase 2) immediately after Wine & Spirits' end-to-end launch (Phase 1) and explicitly before the food-attributes module (Phase 3), and it blocks the most subsequent Phase 2 work (Meilisearch facet design and the Search/Listing/Product-Details API contracts all depend on this module's shape stabilizing first). Purely architectural, per direct instruction: no database design, no API specification, no code, no UI components. Defines why the module exists (five specifications — `01`,`03`,`04`,`05`,`11` — depend on it, with `09` explicitly confirmed as a non-dependent, consuming `food-details` instead), business justification, responsibilities (structured, validated per-product data, tolerant of genuine inapplicability across Wine & Spirits sub-types), explicit non-responsibilities (does not own presentation — the Attributes/Facts boundary `05_PRODUCT_DETAILS_SPECIFICATION.md` §12/§13 already draws; does not hold the "pairs with" relationship, confirming `TIER_B_PRODUCT_RELATIONSHIP_MODULE.md`'s prior correction; does not rank search results; does not determine availability/pricing; does not hold Food Central data), attribute categories (conceptual only — identity/provenance, sensory, serving/storage, compliance-adjacent — no field finalized), scope within Wine & Spirits (whole catalog, not wine alone), a three-way ownership split (engineering/Paul's field-list approval/staff data entry, distinct from the Product Relationship Module's two-way split), integration points with Product, Navigation (the varietal/style navigation layer's actual data source), Search, Listing, Product Details, and Admin Workflows, a consolidated non-integrations section (Cart, Checkout, Account, Food Ordering, Delivery, Homepage all confirmed to have zero dependency), CMS responsibilities, a newly-discovered gap (no document assigns wine-attribute accuracy/verification ownership, distinct from and less severe than `food-details`' already-flagged allergen concern), future extensibility, risks (the field list blocking five specifications' full implementation; the Attributes/Facts boundary being easy to blur), dependencies, a Quality Checklist, and architecture-level Acceptance Criteria (no table, field type, endpoint, or UI component named anywhere, confirmed by direct review). Status intentionally **Draft**, awaiting Paul's review before any refinement pass or approval, per his instruction not to begin another Tier B document after this one until explicitly approved.
- **`docs/implementation-planning/TIER_B_WINE_ATTRIBUTES_MODULE.md` reviewed and finalized to v1.0 — Approved (2026-07-18):** reviewed against `IMPLEMENTATION_PLANNING.md`, `TIER_A_FOUNDATIONAL_RECONCILIATION.md`, `TIER_B_PRODUCT_RELATIONSHIP_MODULE.md`, `MODULE_INVENTORY.md`, `PRODUCT_BLUEPRINT.md`, `BUSINESS_RULES.md`, `MEDUSA_EXTENSIONS.md`, and all 11 frozen specifications, with no existing section rewritten. Genuine findings: two ambiguous self-referencing cross-citations corrected (an unqualified "(§17)" in §4 and §7 that could be misread as pointing to this document's own §17 rather than `TIER_B_PRODUCT_RELATIONSHIP_MODULE.md`'s); one overstated claim corrected (this module "depends on" the Product Relationship Module's architectural decision was softened to "confirms and remains consistent with," since the two modules coexist without a functional dependency); one new non-responsibility added (tasting notes are not a review/rating system, per `05_PRODUCT_DETAILS_SPECIFICATION.md` §22's confirmed no-reviews-in-v1 stance); one new architectural clarification added (whether attributes vary at the Product or Product Variant level — bottle size being the likely candidate — is an explicitly open data-modeling question, not silently assumed); and a corresponding bookkeeping correction made to `TIER_A_FOUNDATIONAL_RECONCILIATION.md` (v1.1) — its baseline table's wine-details dependency list was corrected from `03,04,05,09,11` to `01,03,04,05,11`, since `09_FOOD_ORDERING_SPECIFICATION.md` actually depends on `food-details`, not this module, and `01_NAVIGATION_SPECIFICATION.md`'s Wine Discovery Navigation genuinely does. No contradiction with any frozen specification found. Status changed to **Approved**. Two dependencies newly recorded, not resolved: wine attribute accuracy/verification ownership, and the product-vs-variant granularity question.
- **`docs/implementation-planning/TIER_B_FOOD_ATTRIBUTES_MODULE.md` drafted in full (v1.0, Draft, 2026-07-19)** — the third Tier B document. Before drafting, re-verified `food-details` as the correct next module (per `IMPLEMENTATION_PLANNING.md` §6's explicit "wine-details and food-details... first" ordering) against the six standing selection criteria, rather than assuming the existing baseline was correct — and found the baseline undercounted it: **`food-details` actually has seven frozen-specification dependents (`01`,`02`,`03`,`04`,`05`,`09`,`11`), not the five (`03`,`04`,`05`,`09`,`11`) `TIER_A_FOUNDATIONAL_RECONCILIATION.md` and `MODULE_INVENTORY.md` previously recorded** — `01_NAVIGATION_SPECIFICATION.md`'s Backend Data Requirements table and `02_HOMEPAGE_SPECIFICATION.md`'s Food Central Spotlight (and its own Backend Data Requirements table) both genuinely depend on this module for prep-time/availability data, a dependency neither document had previously reflected. Corrected in `TIER_A_FOUNDATIONAL_RECONCILIATION.md` (bumped to v1.2) and `MODULE_INVENTORY.md` (bumped to v1.3), mirroring the identical bookkeeping-correction precedent `TIER_B_WINE_ATTRIBUTES_MODULE.md`'s own review established. Purely architectural, per direct instruction: no database design, no API specification, no code, no UI components. Defines why the module exists (the corrected seven-dependent count); business justification, naming explicitly that an allergen-data error is a customer-safety failure, not merely a trust/accuracy one — a materially higher stakes framing than `wine-details`' equivalent; responsibilities and explicit non-responsibilities; a dedicated section (§5) stating, per direct instruction, the five-way distinction between attributes, presentation, operational status, inventory, and customer-facing facts — most consequentially that this module never holds Food Central's live availability-state flag (a separate, native, not-yet-built mechanism per `09_FOOD_ORDERING_SPECIFICATION.md` §6/§25), a boundary with no equivalent risk in `wine-details`' own scope; attribute categories; a genuinely discovered field-list gap (portion/serving-size is required by `05_PRODUCT_DETAILS_SPECIFICATION.md` §11 but absent from `PRODUCT_CATALOG.md`'s proposed Food Central field list); scope (safety-critical fields held to a stricter completeness standard than ordinary descriptive attributes, unlike wine's general tolerance for inapplicability; product-vs-variant granularity confirmed as a non-issue for this module, unlike wine's open question); a four-way ownership split (engineering / Paul's field-list approval / staff data entry / the still-open allergen-accuracy-ownership decision, one dimension more than wine's three-way split); integration points with Product, Navigation, **Homepage** (a genuine, confirmed integration — unlike `wine-details`, for which Homepage was confirmed as a non-dependency), Search, Listing, Product Details, and Admin Workflows; a consolidated non-integrations section (Cart, Checkout, Account, Delivery, kitchen operating-hours configuration, and the live availability mechanism itself all confirmed to have zero data-storage overlap with this module); CMS responsibilities; an elevated, consolidated statement of the already-flagged allergen-accuracy-ownership gap (not newly discovered, but named as this module's own central risk for the first time); future extensibility; risks (the wider seven-specification wait on the field list; the Attributes-vs-Operational-Status boundary as a risk specific to this module); dependencies (including the two-decision ownership split and the recommended baseline correction); a Quality Checklist; and architecture-level Acceptance Criteria. Status intentionally **Draft**, awaiting Paul's review before any refinement pass or approval, per his instruction not to begin another Tier B document after this one until explicitly approved.

## Work in progress

**`docs/implementation-planning/TIER_B_FOOD_ATTRIBUTES_MODULE.md` is drafted at v1.0 — Draft, awaiting Paul's review before any refinement pass or approval.** `docs/implementation-planning/TIER_B_WINE_ATTRIBUTES_MODULE.md` and `docs/implementation-planning/TIER_B_PRODUCT_RELATIONSHIP_MODULE.md` remain **Approved** at v1.0. All 11 planned Phase 1 specifications — `01_NAVIGATION_SPECIFICATION.md` through `11_ADMIN_WORKFLOWS_SPECIFICATION.md` — remain **Approved — Frozen** at v1.0. Phase 2's Tier A (Foundational Reconciliation) is complete and Approved (v1.2). `docs/implementation-planning/MODULE_INVENTORY.md` is live and current (v1.3). No Tier C–F document has been drafted. **Per Paul's explicit instruction, no further Tier B document begins until he approves this one.**

## Next recommended task

**Paul to review `docs/implementation-planning/TIER_B_FOOD_ATTRIBUTES_MODULE.md` (v1.0 — Draft) and direct either a refinement pass or a direct freeze**, per `IMPLEMENTATION_PLANNING.md` §7's two-stage pattern — mirroring the process already used for both prior Tier B documents. Two decisions this document specifically surfaces for Paul's attention: the field list (now including the newly-discovered portion/serving-size candidate, §6 of that document) and who is operationally responsible for verifying allergen/ingredient data accuracy — a safety-critical decision, not merely a trust/accuracy one. Beyond this module, `docs/implementation-planning/MODULE_INVENTORY.md` gives a single-page view of the remaining Tier B candidates (delivery-slot scheduling, the local payment provider, the notification provider, Saved-for-Later) for whenever Paul is ready to direct the next one. Phase 0c (component specification) and Phase 1 backend foundation work (per `ROADMAP.md`) similarly await Paul's direction to begin. In parallel: **Paul to resolve the payment-provider decision**, which blocks the start of `ROADMAP.md` Phase 1 (backend foundation); the notification-channel decision (`MEDUSA_EXTENSIONS.md` #5); the data-retention/NDPR-compliance specifics and the account deletion-vs-deactivation policy (`08_CUSTOMER_ACCOUNT_SPECIFICATION.md` §17/§18); the delivery-related open decisions `06_CART_SPECIFICATION.md` §6, `07_CHECKOUT_SPECIFICATION.md` §29, and `10_DELIVERY_SPECIFICATION.md` §28 all surface (delivery mechanism, Lagos-area definition, delivery-slot parameters, cash-on-delivery, hard age-recheck, delivery-fee schedule, failed-delivery-attempt policy, cancellation-cutoff policy, doorstep age verification); the order-status vocabulary, cutoff/slot operational parameters, scheduling horizon, and kitchen operating hours `09_FOOD_ORDERING_SPECIFICATION.md` §28 surfaces; the staff-permission granularity, audit-log retention, report definitions, and alert thresholds `11_ADMIN_WORKFLOWS_SPECIFICATION.md` §28 surfaces; the merchandising decisions `01_NAVIGATION_SPECIFICATION.md` §11/§29 surfaced; formal sign-off on Meilisearch (`MEDUSA_EXTENSIONS.md` #6); and scoping the Saved-for-Later feature (`06_CART_SPECIFICATION.md` §14/§26).

## Blockers

- No Design System blockers remain — `DESIGN_SYSTEM.md` v2.0 is fully approved and frozen.
- **Payment provider is undecided**, which blocks the start of `ROADMAP.md` Phase 1, and blocks final implementation of `07_CHECKOUT_SPECIFICATION.md` §14/§26 (and its Payment State Behaviour section) and `10_DELIVERY_SPECIFICATION.md` once its turn comes.
- No documentation-branch blockers remain — both repository reconciliations above (2026-07-18) resolved every prior unmerged-branch situation; `docs/AI_HANDOFF.md` and `docs/DOCUMENTATION_GOVERNANCE.md` exist and are current as of this update.
- No other hard blockers — architecture and product-catalog work can proceed in parallel with the open questions below, as long as nothing below is assumed in their place.

## Decisions awaiting Paul's approval

Grouped by document, so each can be resolved in context:

**Business rules / delivery (`BUSINESS_RULES.md`, `DELIVERY_MODEL.md`):**
- Whether Wine & Spirits' nationwide delivery uses an in-house fleet, a third-party courier, or both.
- Whether cash-on-delivery is supported at all, given alcohol-specific fraud/reconciliation concerns.
- Which local payment provider is integrated (e.g. Paystack, Flutterwave) — **launch-blocking**.
- Which delivery-update channel(s) are committed to (WhatsApp Business API and/or SMS) — has real cost/approval implications.
- Precision needed for the Lagos delivery-area definition (postal-pattern zones vs. true radius geofencing).

**Trust & checkout (`PRODUCT_BLUEPRINT.md` §9, §11):**
- Exact age-gate mechanics: session duration, and whether it gates the whole site or only alcohol sections.
- Whether a hard compliance re-check happens at order confirmation, in addition to the entry pop-up.
- The alcohol return/refund policy (subject to legal limits on alcohol returns).

**Product data (`PRODUCT_CATALOG.md`, `MEDUSA_EXTENSIONS.md`):**
- Final field list for the wine-attributes module.
- Final field list for the food-attributes module, and who is operationally responsible for keeping allergen/ingredient data accurate. **Newly discovered during `TIER_B_FOOD_ATTRIBUTES_MODULE.md`'s drafting:** `PRODUCT_CATALOG.md`'s proposed Food Central field list does not currently name a portion/serving-size field, despite `05_PRODUCT_DETAILS_SPECIFICATION.md` §11 requiring one — should be carried forward as a named candidate when the field list is finalized, not rediscovered from scratch.
- Operational parameters for delivery slots (slot length, cutoff times, capacity per slot).
- Whether Saved-for-Later (`06_CART_SPECIFICATION.md` §14) is confirmed as in-scope for v1 — a new recommendation, not a pre-approved feature, requiring backend scoping in `MEDUSA_EXTENSIONS.md`.

**Tier A/B findings (`docs/implementation-planning/`):**
- **`TIER_B_FOOD_ATTRIBUTES_MODULE.md` is Draft (v1.0), awaiting Paul's review** — see "Next recommended task," above, for the two decisions it specifically surfaces.
- **`food-details`' true dependency count was undercounted in `TIER_A_FOUNDATIONAL_RECONCILIATION.md` and `MODULE_INVENTORY.md` prior to this review** — corrected from five specifications (`03`,`04`,`05`,`09`,`11`) to seven (`01`,`02`,`03`,`04`,`05`,`09`,`11`), since `01_NAVIGATION_SPECIFICATION.md` and `02_HOMEPAGE_SPECIFICATION.md` both genuinely depend on it for prep-time/availability data. Both tracking documents have been corrected (`TIER_A_FOUNDATIONAL_RECONCILIATION.md` v1.2, `MODULE_INVENTORY.md` v1.3); this is a bookkeeping correction, not a new architectural finding — recorded here for visibility, not left open.
- **`TIER_B_PRODUCT_RELATIONSHIP_MODULE.md` is Approved (v1.0)** — the correction to `PRODUCT_CATALOG.md`'s narrow wine-side-field framing (Tier A §4) is architecturally resolved within the Tier B document itself; `PRODUCT_CATALOG.md` remains unedited, since the correction is a Tier C/data-model-stage action, not a Tier B one.
- **A formal `MEDUSA_EXTENSIONS.md` entry for the Product Relationship Module remains outstanding** — `TIER_B_PRODUCT_RELATIONSHIP_MODULE.md` §20 names this explicitly as deferred to when Tier C or a data-model follow-on document is ready to specify it concretely; recorded here, not resolved.
- The staff-facing curation workflow for the Product Relationship Module is not yet specified anywhere, including in `11_ADMIN_WORKFLOWS_SPECIFICATION.md` — `TIER_B_PRODUCT_RELATIONSHIP_MODULE.md` §7/§15/§20 names this as a genuine open dependency, not resolved by that document.
- **Newly identified during the Tier B finalization review**: `01_NAVIGATION_SPECIFICATION.md` §13/§14/§19 also depends on the Product Relationship Module, for cross-linking/wayfinding rather than a displayed pairing suggestion — a genuine cross-reference gap the original draft omitted, now closed by `TIER_B_PRODUCT_RELATIONSHIP_MODULE.md`'s new "Integration with Navigation" section (a documentation completeness fix, not an open decision).
- See `docs/implementation-planning/MODULE_INVENTORY.md` for the consolidated, single-page view of every module above and its launch-criticality, superseding the need to re-list them individually here going forward.
- **No document assigns operational responsibility for verifying wine attribute accuracy** (correct vintage, ABV, region) once entered — a newly discovered gap, distinct from and less severe than `food-details`' already-tracked allergen-accuracy-ownership question (`TIER_B_WINE_ATTRIBUTES_MODULE.md` §16/§19).
- The wine-attributes module's field list remains the central open decision blocking five specifications' full implementation (`03`,`04`,`05`, plus `01`'s varietal/style navigation layer and `11`'s data-entry workflow) — already tracked under "Product data," above; `TIER_B_WINE_ATTRIBUTES_MODULE.md` makes explicit how much downstream work is waiting on it.
- **Whether any wine attribute (bottle size, most likely) needs Product-Variant-level rather than Product-level representation is undecided** — discovered during `TIER_B_WINE_ATTRIBUTES_MODULE.md`'s finalization review, recorded in that document's §6/§18/§19, not resolved; should be addressed deliberately when the field list is finalized.
- Kitchen operating hours and capacity-driven early closure (`09_FOOD_ORDERING_SPECIFICATION.md` §22) — not yet scoped anywhere.
- Proof of delivery mechanism — confirmation, photo, or signature capture (`10_DELIVERY_SPECIFICATION.md` §17) — not yet scoped anywhere.
- Whether age is physically re-verified at hand-off (`10_DELIVERY_SPECIFICATION.md` §16) — distinct from, but related to, the entry-confirmation/order-confirmation age-recheck question above.
- Staff-facing internal notifications, audit logging, and reports/analytics aggregation (`11_ADMIN_WORKFLOWS_SPECIFICATION.md` §18–§20) — none of the three has any existing scoping in `MEDUSA_EXTENSIONS.md` or `ARCHITECTURE.md`.

**Account & privacy (`08_CUSTOMER_ACCOUNT_SPECIFICATION.md` §17, §18):**
- The exact data-retention period and Nigerian Data Protection Regulation (NDPR) compliance specifics — a legal/business decision, not resolved by the account specification itself.
- The precise account deletion-vs-deactivation policy, including any waiting period and how each interacts with in-progress orders' independent record-keeping.

**Food ordering operations (`09_FOOD_ORDERING_SPECIFICATION.md` §7, §9, §10, §22):**
- The exact customer-facing order-status vocabulary and granularity beyond the minimum progression this specification proposes (Order Received → Preparing → Ready/Out for Delivery → Completed).
- How far in advance a customer may schedule a Food Central order.
- Kitchen operating hours and the conditions under which same-day ordering closes earlier than the stated hours due to capacity.

**Delivery operations (`10_DELIVERY_SPECIFICATION.md` §6, §13, §14, §16, §17, §28):**
- Wine & Spirits' nationwide delivery mechanism (in-house fleet, third-party courier, or both) — shared with the identical open item already tracked under Business rules/delivery, above.
- The failed-delivery-attempt policy, particularly for perishable Food Central orders (number of attempts, what happens after the final attempt, and whether a second same-day attempt, refund, or redelivery charge applies).
- The delivery-cancellation cutoff and policy (a refund, a partial charge, or no cancellation at all past a given fulfillment stage).
- The delivery-fee schedule (flat fee, distance-based, free-above-threshold, or another structure).
- Whether age is physically re-verified at the point of hand-off for an order containing Wine & Spirits items — related to, but distinct from, the hard-recheck-at-order-confirmation question already tracked under Trust & checkout, above.

**Admin operations (`11_ADMIN_WORKFLOWS_SPECIFICATION.md` §6, §14, §15, §18, §19, §20):**
- Who is operationally responsible for verifying allergen/ingredient data accuracy — shared with the identical open item already tracked under Product data, above.
- Whether granular, role-differentiated staff permissions (e.g., kitchen staff vs. dispatch coordinator vs. merchandising) are needed for v1, or whether a single flat admin-user role is sufficient at the company's current operating scale.
- Exact audit-log retention duration and granularity (which staff actions are logged, and for how long).
- Exact report definitions and cadence for staff-facing reporting/analytics.
- Staff-facing alert thresholds (e.g., what stock level counts as "low," how close to capacity triggers a warning).

**Information architecture (`INFORMATION_ARCHITECTURE.md`, `01_NAVIGATION_SPECIFICATION.md`):**
- The exact set of curated/occasion collections for Wine & Spirits (e.g. "Gifting," "Sommelier's Picks") — a merchandising decision.
- The exact category grouping within the Wine & Spirits mega menu (which spirit types nest under a "Spirits" parent vs. stand alone) — `01_NAVIGATION_SPECIFICATION.md` §11 proposes a default to stay within its link budget, but this is a merchandising decision, not a final one.

**Technology sign-off (`TECH_STACK.md`, `MEDUSA_EXTENSIONS.md`):**
- Formal approval of the Next.js Starter storefront recommendation.
- Formal approval of the Meilisearch search recommendation.
- Formal approval (or deferral) of the Sanity CMS recommendation — low urgency.

~~**Design system (`DESIGN_SYSTEM.md`)**~~ — **Approved in full and frozen as v2.0, 2026-07-18.** See `DECISION_LOG.md`.

~~**Brand (`BRAND_IDENTITY.md`)**~~ — **Approved in full, 2026-07-18.** See `DECISION_LOG.md`.

~~**Experience (`EXPERIENCE_PRINCIPLES.md`)**~~ — **Approved in full, 2026-07-18.** See `DECISION_LOG.md`.

**Brand execution (`BRAND_GUIDELINES.md`):**
- Logo, exact typeface selection, exact color tokens, icon set, and physical/operational branding (rider uniforms, packaging, receipts) — no longer blocked by Brand Identity/Experience Principles approval (both are now approved); can begin whenever design capacity is available.

## How to use this section

When a question above is resolved: update the relevant source document, add an entry to `DECISION_LOG.md`, and remove the item from this list in the same change. Do not let this list and the underlying documents drift apart.
