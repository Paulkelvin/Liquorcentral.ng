# Test-only environment values (Next.js does not load .env.local when
# NODE_ENV=test — see https://nextjs.org/docs/pages/guides/environment-variables).
# Never a real key; only satisfies check-env-variables.js for `yarn test`.
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_test_dummy_for_jest
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
NEXT_PUBLIC_DEFAULT_REGION=ng
NEXT_PUBLIC_BASE_URL=http://localhost:8000
